---
phase: 02-host-configured-draft-night
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/feasibility.ts
  - src/core/draw.ts
  - src/core/search.ts
  - tests/core/feasibility.test.ts
  - tests/core/draw.test.ts
  - tests/core/search.test.ts
  - tests/core/roster/fixtures.test.ts
autonomous: true
requirements: [RULE-07, DRFT-02, DRFT-03, DRFT-08, DRFT-09, BAN-08]

must_haves:
  truths:
    - "An empty or non-integer pool size produces a blocking feasibility problem rather than an all-clear"
    - "A configuration that the gate passes can always be drawn without the browser hanging"
    - "Searching `wash` finds Rotom-Wash and searching `aqua` finds Tauros-Paldea-Aqua"
    - "A duplicate ban id counts once, so the legal count never undercounts"
    - "A regulation rotation that changes the roster counts fails a test loudly"
  artifacts:
    - path: "src/core/feasibility.ts"
      provides: "checkFeasibility — 9 blocking codes, 1 warning, declared precedence, set-based counts"
      exports: ["checkFeasibility", "FeasibilityCode", "FeasibilityProblem", "FeasibilityInput", "FeasibilityResult"]
    - path: "src/core/draw.ts"
      provides: "drawPool — two-stage seeded partition draw, always terminating"
      exports: ["drawPool", "DrawInput", "DrawResult"]
    - path: "src/core/search.ts"
      provides: "toSearchKey, matchesName, matchesTypes, matchesMega — the shared pool predicates"
      exports: ["toSearchKey", "matchesName", "matchesTypes", "matchesMega", "MegaFilterMode"]
    - path: "tests/core/roster/fixtures.test.ts"
      provides: "Roster tripwire pinning 235 / 74 / 2 dual-Mega / 18 types"
      contains: "235"
  key_links:
    - from: "src/core/draw.ts"
      to: "src/core/rng.ts"
      via: "nextInt(seed, cursor, max)"
      pattern: "from './rng'"
    - from: "src/core/feasibility.ts"
      to: "src/core/roster/types.ts"
      via: "RosterEntry type import for entries"
      pattern: "roster/types"
---

## Phase Goal

Verbatim from `.planning/ROADMAP.md` §Phase 2 — **note for the developer: this Goal line is
not written in `As a … I want to … so that …` form.** It was not rewritten here, because
inventing a user story the roadmap does not contain would put words in the product owner's
mouth. Run `/gsd mvp-phase 2` if the three-slot form is wanted.

> A host sets up a real tournament for their group — names, format, pool size, banlist —
> and 4–8 friends draft through it on one shared screen with everything they need visible
> at once.

**Slice delivered by this plan:** none that a user can click yet. This is the one sanctioned
exception to MVP vertical-slice ordering, granted by the orchestrator: `feasibility.ts` and
`draw.ts` are the phase's only genuine logic, every later UI surface is a renderer of their
output, and they are testable with zero mocks the moment they exist. Every plan after this
one is a real vertical slice.

<objective>
Build the three pure modules the whole phase renders: the feasibility gate (RULE-07), the
constrained pool draw (DRFT-02 / BAN-08), and the shared pool predicates (DRFT-08 / DRFT-09).

Purpose: the arithmetic is the only place in this phase where being wrong is invisible until
a group is already sat around a screen. Getting it right first, under test, with zero mocks,
is what makes every UI plan after this a rendering exercise.

Output: `src/core/feasibility.ts`, `src/core/draw.ts`, `src/core/search.ts`, their test files,
and a roster tripwire test that fails loudly when regulation M-C rotates in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-host-configured-draft-night/02-CONTEXT.md
@.planning/phases/02-host-configured-draft-night/02-RESEARCH.md
@.planning/phases/02-host-configured-draft-night/02-PATTERNS.md
@.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md
</context>

<interfaces>
<!-- Contracts this plan creates. Later plans consume these exactly as written. -->

`src/core/feasibility.ts`

```ts
export type FeasibilityCode =
  | 'tooFewPlayers'
  | 'blankPlayerName'
  | 'duplicatePlayerName'
  | 'poolSizeNotAnInteger'
  | 'megasExceedRounds'
  | 'tooManyPlayersForRoster'
  | 'poolTooLarge'
  | 'poolTooSmall'
  | 'notEnoughMegas'
  | 'poolExactlyMinimum';

export interface FeasibilityProblem {
  code: FeasibilityCode;
  severity: 'blocking' | 'warning';
  message: string;
}

export interface FeasibilityInput {
  playerNames: readonly string[];
  rounds: number;
  poolSize: number | null;
  megasRequiredPerTeam: number | null;
  bannedIds: readonly string[];
  entries: readonly RosterEntry[];
}

export interface FeasibilityResult {
  blocked: boolean;
  problems: readonly FeasibilityProblem[];
  legalCount: number;
  megaCapableLegalCount: number;
  banCount: number;
}

export function checkFeasibility(input: FeasibilityInput): FeasibilityResult;
export function poolSizeForPreset(players: number, rounds: number, preset: PoolPreset): number;
export type PoolPreset = 'exact' | 'x1_5' | 'x2';
```

`src/core/draw.ts`

```ts
export interface DrawInput {
  candidates: readonly RosterEntry[];
  size: number;
  megasRequired: number;
  seed: number;
  cursor?: number;
}
export interface DrawResult {
  ids: string[];
  megaCapableCount: number;
  cursor: number;
}
export function drawPool(input: DrawInput): DrawResult;
```

`src/core/search.ts`

```ts
export type MegaFilterMode = 'all' | 'mega' | 'nonMega';
export function toSearchKey(text: string): string;
export function matchesName(entry: RosterEntry, normalizedQuery: string): boolean;
export function matchesTypes(entry: RosterEntry, selected: readonly string[], matchAll: boolean): boolean;
export function matchesMega(entry: RosterEntry, mode: MegaFilterMode): boolean;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkFeasibility — the ten-case gate with the NaN hole closed</name>
  <files>src/core/feasibility.ts, tests/core/feasibility.test.ts</files>

  <read_first>
    - `src/core/migrate.ts` (whole file, 72 lines) — the result-object posture and the
      `Number.isSafeInteger` refusal at `:54-58` that F-08 and F-09 copy verbatim in spirit.
    - `src/core/reduce.ts:131-184` — `canApply`'s declared, ordered sequence of checks. Note
      it returns the FIRST failure; `checkFeasibility` deliberately differs and collects all.
    - `src/core/import-guard.ts:190-196` — `isNonNegativeInteger` / `isPositiveInteger`. Copy
      the SHAPE of these helpers as module-private functions; do not import across modules.
    - `src/core/roster/types.ts` — `RosterEntry` (`:84`), specifically `id`, `name`, `types`,
      `megaCapable`, `baseStats`.
    - `src/core/selectors.ts:40-54` — the house set-membership pattern
      (`new Set(...)` built inside a function, never stored).
    - `.planning/phases/02-host-configured-draft-night/02-RESEARCH.md` §Feasibility Arithmetic
      (symbols, ceilings, ban-starvation tables, F-01…F-10, §Precedence order, revised) and
      §Code Examples "The gate's set-based counts (F-10)".
    - `.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md` §Copywriting Contract →
      "Feasibility reasons (RULE-07)" — the eight literal strings, and §5 Feasibility bar.
    - `tests/core/selectors.test.ts` (whole file) — module-level fixture constants, real roster
      ids, zero mocks, no import of `src/store.ts`, no environment comment.
  </read_first>

  <behavior>
    - `poolSize: null` → `blocked === true`, `problems[0].code === 'poolSizeNotAnInteger'`.
    - `poolSize: Number.NaN` → same. `NaN > legal` and `NaN < p*r` are both `false`, so this
      is the case that must not fall through to an all-clear.
    - `poolSize: 48.5` → `poolSizeNotAnInteger`. `poolSize: 0` → `poolSizeNotAnInteger`.
    - `megasRequiredPerTeam: null`, `7` with `rounds: 6`, `-1`, `2.5` → `megasExceedRounds`.
    - `megasRequiredPerTeam: 9`, `rounds: 6`, `playerNames: ['Ada','Bo']` → the FIRST problem
      is `megasExceedRounds`, not `notEnoughMegas` (18 ≤ 74 so F-06 would pass).
    - `playerNames: ['Ada']` → `tooFewPlayers`.
    - `playerNames: ['Ada','Bo','  ']` → `blankPlayerName`, message names `Player 3`.
    - `playerNames: ['Sam','sam ']` → `duplicatePlayerName`. Normalization is trim, lowercase,
      collapse internal whitespace runs to one space. `['Ada  Lovelace','ada lovelace']` collides.
    - 40 players, `rounds: 6`, `poolSize: 240`, no bans → `tooManyPlayersForRoster`, NOT
      `poolTooLarge`. 39 players / `poolSize: 234` → not blocked.
    - `bannedIds: ['pikachu','pikachu']` → `banCount === 1` and `legalCount === 234`.
    - `bannedIds: ['not-a-real-id']` → `banCount === 0`, `legalCount === 235`.
    - 8 players, `rounds: 6`, `poolSize: 12` → `poolTooSmall`; `poolSize: 236` → `poolTooLarge`.
    - 8 players, `megasRequiredPerTeam: 6`, 27 Mega-capable ids banned → `notEnoughMegas`
      (74 − 27 = 47 < 48).
    - 2 players, `rounds: 6`, `poolSize: 12` → `blocked === false` and
      `problems.map(p => p.code)` equals `['poolExactlyMinimum']` with `severity: 'warning'`.
    - When several problems hold, `problems` is ordered by the declared `PRECEDENCE` array.
    - Every `message` matches the 02-UI-SPEC string byte for byte after interpolation.
    - `poolSizeForPreset(8, 6, 'exact') === 48`, `'x1_5' === 72`, `'x2' === 96`; the ×1.5
      formula uses `Math.ceil` so Phase 3's odd round counts inherit a rounding rule.
  </behavior>

  <action>
Create `src/core/feasibility.ts` as a pure module. It may import only
`type { RosterEntry } from './roster/types'`. It must contain no DOM, clock, randomness,
network, storage or timer token — `npm run check:pure` fails the build otherwise.

Open with an S-2 doc block that states the DECISION, not the mechanics: this module is the
single authority on what is satisfiable (D-06 applied one level down), it takes
`entries` rather than pre-computed counts so a caller cannot pass an inconsistent
`(legalCount, megaCapableLegalCount)` pair, and it types `poolSize` and
`megasRequiredPerTeam` as `number | null` so an empty numeric field is a case TypeScript
forces the gate to handle rather than a value that silently satisfies every comparison.

Export the interfaces exactly as given in `<interfaces>` above. Declare `PRECEDENCE` as a
module-level `readonly FeasibilityCode[]` in this order — the order is declared, not
emergent, and `problems` is sorted by it:

`tooFewPlayers`, `blankPlayerName`, `duplicatePlayerName`, `poolSizeNotAnInteger`,
`megasExceedRounds`, `tooManyPlayersForRoster`, `poolTooLarge`, `poolTooSmall`,
`notEnoughMegas`, `poolExactlyMinimum`.

This order is a DELIBERATE DEVIATION from 02-UI-SPEC §5's seven-item list. Record the
deviation in the doc block with its three reasons, all from 02-RESEARCH: `poolSizeNotAnInteger`
(F-08) and `megasExceedRounds` (F-09) are malformed input rather than unsatisfiable arithmetic
and must be reported before any sentence computed from a malformed number; and
`tooManyPlayersForRoster` exists because at the Exact preset `N === p × r` identically, so
`poolTooSmall` can never fire and a 40-player host would otherwise be told "Pool is too large"
when the fix is fewer players.

Predicates, using the symbols `p = playerNames.length`, `r = rounds`, `N = poolSize`,
`k = megasRequiredPerTeam`, `L = legalCount`, `M_L = megaCapableLegalCount`:

- `tooFewPlayers`: `p < 2`
- `blankPlayerName`: any `playerNames[i].trim() === ''`; message names the 1-based index
- `duplicatePlayerName`: two names share a normalized form; normalization is
  `name.trim().toLowerCase().replace(/\s+/g, ' ')`. Put this in a module-private
  `normalizePlayerName` with a doc comment recording WHY: a screen reader cannot distinguish
  `Sam` from `sam `, and neither can the pick table. Message quotes the first player's
  name as typed.
- `poolSizeNotAnInteger`: `N === null || !Number.isSafeInteger(N) || N < 1`
- `megasExceedRounds`: `k === null || !Number.isSafeInteger(k) || k < 0 || k > r`
- `tooManyPlayersForRoster`: `p * r > L`
- `poolTooLarge`: `N > L` (skipped when `poolSizeNotAnInteger` or `tooManyPlayersForRoster` holds)
- `poolTooSmall`: `N < p * r` (skipped when `poolSizeNotAnInteger` holds)
- `notEnoughMegas`: `p * k > M_L`
- `poolExactlyMinimum`: `N === p * r` — severity `warning`, never blocking

Compute `legalCount`, `megaCapableLegalCount` and `banCount` as SET operations, exactly as
02-RESEARCH §Code Examples "The gate's set-based counts (F-10)" gives them: build
`const banned = new Set(input.bannedIds)` and reduce over `input.entries`; `banCount` is
`entries.length - legalCount`, i.e. the number of bans that HIT the roster, which is the
figure every message quotes. Never `bannedIds.length`. The `Set` is computation-local and is
never returned or stored (CLAUDE.md §Serializability).

`blocked` is `problems.some(p => p.severity === 'blocking')`.

Message strings are fully interpolated inside this module and are verbatim from
02-UI-SPEC §Copywriting Contract → "Feasibility reasons (RULE-07)". Hold them as module
constants with interpolation helpers per S-5 — do not build them at the call site, and do not
let a component compose one. The UI renders `problems[0].message` and a count of the rest.

Also export `poolSizeForPreset(players, rounds, preset)` returning `players * rounds` for
`exact`, `Math.ceil(players * rounds * 1.5)` for `x1_5`, `players * rounds * 2` for `x2`.
Note in its doc comment that at `r = 6` all three are integers for every `p`, so the
`Math.ceil` is unreachable today and exists so Phase 3's variable round count inherits a
rounding rule rather than discovering it.

Write `tests/core/feasibility.test.ts` in the `tests/core/selectors.test.ts` house style:
module-level fixture constants, real roster ids read from `public/data/roster.mb.json`, zero
mocks, no import of `src/store.ts`, and NO `@vitest-environment` comment (the default is
`node`). Cover every bullet in `<behavior>`.
  </action>

  <verify>
    <automated>npx vitest run tests/core/feasibility.test.ts && npm run check:pure</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/core/feasibility.test.ts` exits 0.
    - `npm run check:pure` exits 0.
    - `grep -c "export function checkFeasibility" src/core/feasibility.ts` returns 1.
    - `grep -c "tooManyPlayersForRoster" src/core/feasibility.ts` returns at least 3
      (type member, PRECEDENCE entry, predicate).
    - A test named for the NaN hole exists and asserts that
      `checkFeasibility({ playerNames: ['Ada','Bo'], rounds: 6, poolSize: Number.NaN,
      megasRequiredPerTeam: 0, bannedIds: [], entries: ENTRIES })` returns
      `blocked === true` and `problems[0].code === 'poolSizeNotAnInteger'`.
    - A test asserts `checkFeasibility` with `bannedIds: ['pikachu','pikachu']` returns
      `banCount === 1` and `legalCount === 234`.
    - A test asserts 40 players at `poolSize: 240` yields `problems[0].code ===
      'tooManyPlayersForRoster'` and NOT `'poolTooLarge'`.
    - A test asserts 2 players at `poolSize: 12` yields `blocked === false` and
      `problems.map(p => p.code)` deep-equals `['poolExactlyMinimum']`.
    - `grep -v '^ \*' src/core/feasibility.ts | grep -vc "bannedIds.length"` confirms the
      module never computes a count from the array length: `grep -c "bannedIds.length" src/core/feasibility.ts`
      returns 0.
    - `grep -Ec "document|window|localStorage|Math.random|Date\.now|setTimeout" src/core/feasibility.ts` returns 0.
  </acceptance_criteria>

  <done>`checkFeasibility` returns ten precedence-ordered problem codes with verbatim UI-SPEC copy, refuses `null`/`NaN`/non-integer numeric fields before any arithmetic reason, and counts bans by set membership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drawPool — the two-stage partition draw</name>
  <files>src/core/draw.ts, tests/core/draw.test.ts</files>

  <read_first>
    - `src/core/rng.ts` (whole file, 42 lines) — `nextInt(seed, cursor, max)` returns
      `{ value, cursor }` and THROWS `RangeError` for `max < 1`. `drawPool` inherits that
      throw rather than clamping (S-3's one sanctioned exception).
    - `src/core/selectors.ts:135-152` — `selectStartingOrder`, the only existing `rng`
      consumer and the only existing Fisher-Yates. Copy three things verbatim: `readonly`
      input with a freshly built output (`[...playerIds]`), `cursor` as a local `let` threaded
      through every `nextInt` call, and the `if (x === undefined) continue;` guard on every
      indexed read (`noUncheckedIndexedAccess` is on — these are required, not defensive habit).
    - `src/core/selectors.ts:34-43` — `selectAvailablePool`'s doc comment: "the pool ids are
      built in display order, so a filter that reordered them would reshuffle the grid under
      the host's cursor on every pick." This is the ordering contract Task 2 must not break.
    - `src/app.tsx:101-116` — `byDexOrder`, the display order. Rotom and its five appliances
      all share `num` 479, which is why the tie-break on `id` exists.
    - `.planning/phases/02-host-configured-draft-night/02-RESEARCH.md` §The Constrained Draw
      (the hypergeometric table, §Two-stage partition draw — recommended, §Ordering matters and
      is easy to get wrong) and §Code Examples "Two-stage seeded draw".
    - `.planning/phases/02-host-configured-draft-night/02-PATTERNS.md` §`src/core/draw.ts`.
  </read_first>

  <behavior>
    - Same `(candidates, size, megasRequired, seed)` twice → identical `ids` arrays.
    - `ids.length === size` for every tested configuration.
    - `ids` is a subsequence of `candidates.map(e => e.id)` — the draw selects a SET and emits
      it in the candidates' order, never the shuffle's.
    - `megasRequired: 0` → stage 2 selects nothing; the result is a plain uniform draw and is
      still deterministic from the seed.
    - 235 real candidates, `size: 48`, `megasRequired: 32` → `megaCapableCount >= 32`, and
      the call returns in under 100ms. This is the 8-player / k=4 / Exact configuration that
      needs ~6.4 × 10⁷ redraws under rejection sampling; it must not hang.
    - `megaCapableCount` equals the number of returned ids whose entry has `megaCapable === true`.
    - `cursor` returned equals `(input.cursor ?? 0) + size`.
    - `candidates` is not mutated: `JSON.stringify(candidates)` is unchanged after the call.
    - `size > candidates.length` throws `RangeError` (surfaced from `nextInt`, not swallowed).
    - Two different seeds over the same candidates produce different `ids` (assert with two
      fixed seeds, not random ones).
  </behavior>

  <action>
Create `src/core/draw.ts` as a pure module importing only `{ nextInt } from './rng'` and
`type { RosterEntry } from './roster/types'`. Nothing else.

Open with an S-2 doc block that states the decision and carries 02-RESEARCH's honest caveat
VERBATIM in substance: reject-and-redraw was rejected because at 8 players / 4 Megas required /
Exact pool the probability a uniform 48-draw satisfies the constraint is 1.56 × 10⁻⁸ — about
64 million expected redraws — and that configuration passes every feasibility blocker, so it
is reachable by an ordinary host; a browser freeze there is a correctness bug wearing a
performance disguise. The two-stage draw is O(L) and always terminates. Then state the caveat
that is being ACCEPTED rather than fixed (02-RESEARCH assumption A4): the draw IS uniform when
`megasRequired === 0`, which is the default and the overwhelmingly common case, and is NOT
uniform over the constraint-satisfying set when it is greater — a Mega-heavy pool has more
distinct (stage-2, stage-3) paths that produce it, so it is over-weighted. Do not attempt to
fix this; uniformity over the constrained set requires the rejection sampling the first
paragraph rules out.

Implement a module-private `selectInPlace(pool, count, seed, cursor)` returning
`{ taken, cursor }` — a partial Fisher-Yates over a working copy the caller owns, with the
`undefined` guards on both indexed reads. Then `drawPool`:

1. Partition `candidates` into `megaCapable` and `rest` with `Array.prototype.filter`,
   preserving the input's display order inside each part.
2. Stage 2 — draw `megasRequired` from a copy of `megaCapable`.
3. Stage 3 — draw `size - megasRequired` from `[...megaCapable not already chosen, ...rest]`.
4. Emit `candidates.filter(e => chosen.has(e.id)).map(e => e.id)`.

Step 4 is the whole of Pitfall 4 and the single easiest thing to get wrong: returning the
shuffle's prefix directly compiles, passes a length assertion, and produces a pool grid in
random order with Rotom's five appliances scattered across it. Put a comment on that line
pointing at `selectors.ts:36-39`.

`megaCapableCount` is counted from the CHOSEN set against `entry.megaCapable`, not from
`megasRequired` — it is usually larger, because stage 3 draws from a set that still contains
unused Mega-capable entries. It is D-09's recorded figure and Phase 3's RULE-09 gate reads it
rather than recomputing against a rotated roster; say so in the field's doc comment.

Return the advanced `cursor` so a caller that draws twice cannot reuse the stream. Note in the
doc block that Phase 2's two config-time derivations (pool draw, starting order) use two
INDEPENDENT seeds rather than one advancing cursor, so this field is provenance rather than a
sharing mechanism — see `src/store.ts:162-166`, which predicted this collision one phase early.

Write `tests/core/draw.test.ts` in the house style, using the real committed roster
(`public/data/roster.mb.json`) sorted by the same dex order the app uses, covering every
bullet in `<behavior>`. The 100ms timing assertion is a real assertion, not a comment.
  </action>

  <verify>
    <automated>npx vitest run tests/core/draw.test.ts && npm run check:pure</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/core/draw.test.ts` exits 0.
    - `npm run check:pure` exits 0.
    - `grep -c "export function drawPool" src/core/draw.ts` returns 1.
    - `grep -Ec "^import" src/core/draw.ts` returns 2, and both are `./rng` and `./roster/types`.
    - A test asserts `ids` is a subsequence of `candidates.map(e => e.id)` — implemented as a
      forward two-cursor walk, not as a set comparison, so shuffle order actually fails it.
    - A test named for the 8-player / 4-Mega / Exact case asserts the call completes with
      `megaCapableCount >= 32` and measured elapsed time under 100ms.
    - A test asserts `JSON.stringify(candidates)` is byte-identical before and after `drawPool`.
    - A test asserts `drawPool({...}).cursor === size` when `cursor` is omitted.
    - `grep -Ec "Math.random|document|window|localStorage|Date\.now" src/core/draw.ts` returns 0.
    - The module doc block contains the phrase `not uniform over the constraint-satisfying set`.
  </acceptance_criteria>

  <done>`drawPool` selects a set in O(L) with exactly `size` rng draws, always terminates, emits ids in the candidates' display order, and records the drawn Mega-capable count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the shared pool predicates, and the roster tripwire</name>
  <files>src/core/search.ts, tests/core/search.test.ts, tests/core/roster/fixtures.test.ts</files>

  <read_first>
    - `src/core/roster/types.ts:30-40` — `RawSpecies.id` documented as "Showdown `toID`:
      lowercase, `[^a-z0-9]+` stripped. THE identity key" — and `:84` `RosterEntry` with
      `types: string[]` and `megaCapable: boolean`.
    - `src/core/selectors.ts:40-54` — a small exported predicate plus a module-private helper,
      the structural pattern this module copies.
    - `tests/ui/sprite-resolution.test.ts:1-31` — the data-fixture model: imports
      `../../public/data/roster.mb.json` directly and asserts against the REAL committed data.
      No `@vitest-environment` line, so it runs in `node`.
    - `tests/core/roster/fixtures.test.ts` (whole file) — the file being extended.
    - `.planning/phases/02-host-configured-draft-night/02-RESEARCH.md` §Filter and search
      recomputation cost (the `toID(entry.name) === entry.id` finding, 0 mismatches across all
      235 entries and all 76 Mega formes) and §Pattern 2 "One predicate, two consumers".
    - `.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md` §8 Filter bar (D-32, D-33,
      D-34).
    - `CLAUDE.md` §Conventions → Identity: never `split('-')` a species name.
      `Kommo-o`, `Mr. Rime`, `Tauros-Paldea-Aqua`, `Rotom-Wash` are the fixtures that punish it.
  </read_first>

  <behavior>
    - `toSearchKey('Mr. Rime') === 'mrrime'`; `toSearchKey('mr rime') === 'mrrime'`;
      `toSearchKey('Tauros-Paldea-Aqua') === 'taurospaldeaaqua'`; `toSearchKey('Kommo-o') === 'kommoo'`.
    - `matchesName(entry, '')` is `true` for every entry.
    - Query `rotom` matches Rotom and all five appliances — exactly 6 entries in the committed roster.
    - Query `aqua` matches `Tauros-Paldea-Aqua`. Query `wash` matches `Rotom-Wash`. Neither is
      a prefix match, which is the whole reason D-32 rejected prefix matching.
    - `matchesTypes(entry, [], false)` and `matchesTypes(entry, [], true)` are both `true`
      (no selection is no constraint).
    - `matchesTypes` with `['Water','Flying']` and `matchAll: false` is OR; with
      `matchAll: true` it is AND and requires the entry to carry both.
    - `matchesMega(entry, 'all')` is always `true`; `'mega'` is `entry.megaCapable`;
      `'nonMega'` is its negation.
    - Tripwire: the committed roster has 235 entries, 74 Mega-capable, exactly 2 species with
      `megaFormes.length > 1` whose ids sorted are `['charizard','raichu']`, 18 distinct types,
      base-stat totals within 288–600, no single stat above 200, longest `name` 20 characters,
      and `toSearchKey(entry.name) === entry.id` for all 235 entries and all Mega formes.
  </behavior>

  <action>
Create `src/core/search.ts` — the pool predicates, all four in one module so two consumers
cannot drift. It imports only `type { RosterEntry } from './roster/types'`.

`toSearchKey(text)` is `text.toLowerCase().replace(/[^a-z0-9]+/g, '')` — Showdown's `toID`.
`matchesName(entry, normalizedQuery)` returns `true` for an empty query, otherwise
`toSearchKey(entry.name).includes(normalizedQuery)`. Normalize the QUERY once per keystroke at
the call site, never once per entry; say so in the doc comment and name both consumers
(the DRFT-08 pool search in `FilterBar` and the ban `TypeaheadField`), per 02-UI-SPEC §4:
"one predicate, one module, one test suite. Not a second matcher that can drift."

Write `matchesName` against `entry.name`, NOT against `entry.id`, even though 02-RESEARCH
verified they normalize identically today. The equality is an optimization, not a correctness
dependency, and the tripwire test below is what makes a future regulation break it loudly
instead of search silently missing a species. Record that reasoning in the doc comment.

`matchesTypes(entry, selected, matchAll)`: empty `selected` → `true`; `matchAll` → every
selected type is in `entry.types`; otherwise some selected type is in `entry.types`. Note that
zero entries in the committed roster carry more than two types, so the inner loops are bounded
at 2 — verified, not assumed.

`matchesMega(entry, mode)` over `MegaFilterMode`, declared as a string-literal union with a
doc comment per member (S-4). Its doc block must record that Phase 3 composes the round's own
restriction as a SEPARATE predicate rather than a fourth member of this union, which is what
lets DRFT-09's "composing with the round's own restriction" arrive without reshaping the
control (02-UI-SPEC §Deferred to Phase 3).

Nowhere in this module may a species name be split, sliced or indexed by separator.

Write `tests/core/search.test.ts` in the house style against the real committed roster.

Extend `tests/core/roster/fixtures.test.ts` with a `describe('roster tripwire — regulation
M-B', …)` block asserting every tripwire bullet above. Give the block a comment stating its
job explicitly: the snapshot's `validUntil` is 2026-09-02, the roster rotates roughly every
2.5 months, and every feasibility ceiling in this phase is regulation-specific — so a
regeneration must announce itself as a loud test failure rather than as a subtly wrong gate
(Pitfall 9). Do NOT put these numbers in `src/`; nothing in `src/` may hardcode a roster
figure (D-17).
  </action>

  <verify>
    <automated>npx vitest run tests/core/search.test.ts tests/core/roster/fixtures.test.ts && npm run check:pure</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/core/search.test.ts tests/core/roster/fixtures.test.ts` exits 0.
    - `npm run check:pure` exits 0.
    - `grep -Ec "export function (toSearchKey|matchesName|matchesTypes|matchesMega)" src/core/search.ts` returns 4.
    - `grep -c "split(" src/core/search.ts` returns 0.
    - A test asserts a query of `rotom` matches exactly 6 committed entries.
    - A test asserts `matchesName` finds `Tauros-Paldea-Aqua` from the query `aqua` and
      `Rotom-Wash` from the query `wash`.
    - `tests/core/roster/fixtures.test.ts` contains assertions with the literals `235`, `74`,
      `18`, `288`, `600`, `20`, and an assertion that the dual-Mega species ids sorted deep-equal
      `['charizard', 'raichu']`.
    - A test asserts `toSearchKey(entry.name) === entry.id` across all committed entries with
      zero mismatches.
    - `grep -Erc "\b235\b|\b74\b" src/ --include=*.ts --include=*.tsx` returns 0 for every file
      (no roster figure is hardcoded in source).
  </acceptance_criteria>

  <done>One module exports the name, type and Mega predicates used by both the pool filter and the ban typeahead, and a tripwire test pins the committed roster's counts so a regulation rotation fails loudly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-typed config → `checkFeasibility` | Free numeric and text fields cross here. The values are not hostile, but they are unconstrained: `<input type="number">` yields `NaN` on empty and `max="6"` does not prevent typing 9. |
| imported/migrated document → `checkFeasibility` | A `bans` array from a JSON file may contain duplicates or ids no longer in the roster. |
| host config → `drawPool` | The only compute-bound operation in the phase, run synchronously on the host's own main thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | `src/core/draw.ts` | mitigate | Two-stage partition draw, O(L) with exactly `size` rng draws and no loop bound. Rejection sampling is prohibited by the module doc block. Pinned by a <100ms assertion at the 8-player / k=4 / Exact configuration that would need ~6.4 × 10⁷ redraws. |
| T-02-02 | Tampering | `src/core/feasibility.ts` | mitigate | `legalCount` and `megaCapableLegalCount` are set differences against the roster id set, so a duplicated or non-roster ban id cannot make the gate report a smaller legal pool than exists. Asserted directly. |
| T-02-03 | Tampering | `src/core/feasibility.ts` | mitigate | `poolSize` and `megasRequiredPerTeam` are typed `number | null` and refused unless `Number.isSafeInteger`, so `NaN`, `Infinity`, `48.5` and `4e9` are all blocking problems before any arithmetic reason is computed. |
| T-02-04 | Information disclosure | all three modules | accept | Pure functions over roster data the app already ships publicly. No secret, no network, no storage, no PII. |
| T-02-SC | Tampering | npm installs | accept | This plan installs nothing. `package.json` is not modified. 02-RESEARCH §Package Legitimacy Audit records the phase installs no packages; a proposed `npm install` here is a C-1 constraint violation to escalate, not a checkpoint to gate. |
</threat_model>

<verification>
- `npm run verify` exits 0 (`check:pure`, `check:nohtml`, `test`, `build`).
- `npm run check:pure:selftest` exits 0 — the purity checker still detects a violation.
- `git diff --stat package.json` is empty: no dependency was added.
- `npx vitest run tests/core/` exits 0 with the three new suites plus the extended fixtures suite.
</verification>

<success_criteria>
- `checkFeasibility` returns precedence-ordered problems with verbatim 02-UI-SPEC copy, refuses
  `null`/`NaN`/non-integer numeric input, and counts bans by set membership.
- `drawPool` is deterministic from its seed, emits ids in display order, always terminates, and
  records the drawn Mega-capable count for Phase 3.
- `src/core/search.ts` is the single home of the name, type and Mega predicates.
- The roster tripwire pins 235 / 74 / 2 dual-Mega / 18 types against the committed snapshot.
- No file under `src/` hardcodes a roster figure.
</success_criteria>

<output>
Create `.planning/phases/02-host-configured-draft-night/02-01-SUMMARY.md` when done.
</output>

