---
phase: 02-host-configured-draft-night
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/ui/components/SplitPanes.tsx
  - src/ui/components/SplitPanes.css
  - tests/ui/draft-panes.test.tsx
  - .planning/phases/02-host-configured-draft-night/02-UI-SPEC.md
  - .planning/phases/02-host-configured-draft-night/02-UAT.md
autonomous: true
requirements: [DRFT-10, DRFT-14]

must_haves:
  truths:
    - "While a draft is running the pool pane shows an Expand the pool control that is visibly unavailable and says why, instead of showing nothing"
    - "Clicking that control mid-draft does nothing: the pane stays split, the board stays on screen, nothing is announced and nothing is written to storage"
    - "Once the draft is complete the same control works and its reason is gone"
    - "The pool pane's content and the board pane's content start on the same line, whether or not either chrome slot holds a control"
    - "02-UI-SPEC.md no longer says the pool expand button is unrendered mid-draft, and no longer contradicts its own always-rendered convention"
    - "02-UAT.md test 9 states the scoped truth, and test 16 carries a setup step that makes its own assertion observable"
  artifacts:
    - path: "src/ui/components/SplitPanes.tsx"
      provides: "The inert pool expand control and its reason"
      contains: "aria-disabled"
    - path: "src/ui/components/SplitPanes.css"
      provides: "Reserved chrome height and the inert/reason styling"
      contains: "min-height: var(--target-min)"
    - path: "tests/ui/draft-panes.test.tsx"
      provides: "The amended pin: present-but-inert, and the click refused"
      contains: "aria-disabled"
  key_links:
    - from: "src/ui/components/SplitPanes.tsx"
      to: "the expandable prop"
      via: "early return in the click handler"
      pattern: "if \\(!expandable\\) return"
    - from: "src/ui/components/SplitPanes.tsx"
      to: "the reason element"
      via: "aria-describedby"
      pattern: "aria-describedby"
---

<objective>
Close both UAT gaps recorded in `02-UAT.md`. Neither is a broken feature — in both cases the
code matches its spec and the defect is elsewhere.

Gap 1 is a missing affordance. `poolExpandable = complete` is correct and stays exactly as it
is; what is wrong is that the rule is enforced by pure omission, so the pool's chrome slot
renders empty mid-draft and the host cannot tell "unavailable" from "broken". Build the inert
control with a reason, and reserve the chrome height so the two panes align.

Gap 2 needs no code at all. The bans disclosure is correct and its tests pass; UAT test 16
inherits a zero-ban tournament and then asserts a count, which is unobservable as written.
Fix the test document.

Purpose: a control that appears and disappears is worse on a shared screen than one that is
predictably inert — the spec's own words, which this one surface was the exception to.
Output: an inert pool expand control, aligned panes, an amended spec, an amended UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@src/ui/components/SplitPanes.tsx
@src/ui/components/SplitPanes.css
@tests/ui/draft-panes.test.tsx

<!-- The inert precedent to match. Do not invent one; this is established, in 02-04. -->
@src/ui/components/FeasibilityBar.tsx
@src/ui/components/FeasibilityBar.css
</context>

<constraints>
These bound the whole plan. Violating any one reopens a closed threat or a ROADMAP criterion.

1. **`src/app.tsx` line 501, `const poolExpandable = complete;` DOES NOT CHANGE.** Not in
   this plan, not incidentally. `src/app.tsx` is deliberately absent from `files_modified`.
2. **Both T-02-24 coercions stay.** `src/adapters/view-prefs.ts` line 94 (the union check)
   and `src/app.tsx` line 519 (the stored-pool-to-split coercion). Neither file is touched.
3. **The control must be INERT, not functional.** A working pool expand mid-draft lets
   pool-full collapse the board to a 44px strip, regressing ROADMAP Phase 2 criterion 5 ("at
   every moment the shared screen shows ... the full players x rounds board") and reopening
   T-02-24, recorded `closed` at `02-SECURITY.md:89`.
4. **No behavioural change to either rule.** The pool still does not expand mid-draft; the
   bans disclosure still does not render at zero bans. Only the affordance changes.
5. **No new dependency.** Runtime dependencies stay exactly `preact` and `@preact/signals`.
6. **Scope.** Do not touch the pane state model, the security posture, `TopBar.tsx`, or any
   spec section beyond the four edits named in Task 2.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the pool expand control inert, and repin the test that asserted its absence</name>
  <files>src/ui/components/SplitPanes.tsx, src/ui/components/SplitPanes.css, tests/ui/draft-panes.test.tsx</files>

  <behavior>
    - Mid-draft, split view: the pool chrome holds a button named exactly `Expand the pool`
      carrying `aria-disabled="true"` and NO native `disabled`, plus a reason element it
      points at with `aria-describedby`.
    - Mid-draft, split view: clicking that button leaves `data-pane` at `split`, announces
      nothing, and writes nothing to storage.
    - Mid-draft, board expanded: the pool is collapsed and its chrome holds exactly one
      button, `Show the pool`. No inert control and no reason on a collapsed strip.
    - Draft complete, split view: the same button carries no `aria-disabled`, no reason
      element exists, and clicking it sets `data-pane` to `pool`.
    - The board's expand control is never inert — `side` is called for it with `expandable`
      literally `true`.
  </behavior>

  <action>
    ## Why the component change and the test amendment are ONE task

    They must land in one TASK, and the plan cannot split them across two, because
    `tests/ui/draft-panes.test.tsx:251-257` currently asserts
    `expect(buttonNamed('Expand the pool')).toBeUndefined()`. `buttonNamed()` matches on
    `textContent`, not on `aria-disabled`, so the moment the control renders — inert or not —
    that assertion fails. `npm run verify` runs `vitest run` unscoped, so a task boundary
    between the TSX change and the test amendment would put a DONE gate in front of a
    knowingly red suite.

    One task, not necessarily one commit. This task carries `tdd="true"`, so the executor
    protocol splits it into a RED commit (test-only, expected to fail) and a GREEN commit
    (implementation, expected to pass). That is fine and is not the thing this section
    guards against: both commits land before the task's own `<verify>` runs, and that
    single end-of-task gate is the only point where green is required. What must not happen
    is a second TASK boundary in between.

    Order within the task is yours; the gate is at the end either way.

    ## src/ui/components/SplitPanes.tsx

    **Add one copy constant** beside the existing block at lines 34-41, which this file
    already holds as verbatim spec constants:

    `const POOL_EXPAND_REASON = 'Available once the draft is complete';`

    That exact string. No trailing period — it is a fragment annotating a control, the same
    shape as the `Not yet available` suffix on the ban-mode options at
    `ConfigScreen.tsx:157-158`. The em dash separator is NOT part of this string; CSS
    supplies it, for the reason given in the stylesheet section below.

    **Give `side()` an eighth parameter,** `reason: string | null`, placed last. Positional,
    like the seven before it. Do NOT refactor the signature into an options object — every
    piece of copy this function renders is already a positional parameter, and consistency
    with the existing call sites is worth more than the tidier signature here.

    **Inside `side()`,** derive a stable id for the reason element from the existing `key`
    parameter, of the form pool-expand-reason / board-expand-reason. Derived rather than a
    module constant so the function stays honest about being generic, even though only the
    pool can currently reach the inert branch.

    **Rewrite the non-collapsed branch of the chrome.** Today it renders the expand button
    only when `!isFullWidth && expandable`, so an unexpandable pool renders nothing at all —
    that omission is the entire defect. The fix drops `expandable` from the membership test
    and moves it to the STATE of the control:

    - Condition to render at all: `!isFullWidth`. An expanded pane still carries no control
      of its own; the restore lives on the collapsed strip opposite. That is unchanged.
    - The button keeps `type="button"`, `class="pane__button"` and the label `{expandLabel}`.
    - Add `aria-disabled={expandable ? undefined : 'true'}`.
    - Add `aria-describedby` pointing at the reason id, but only when the control is inert
      and a reason was supplied; otherwise `undefined`.
    - Do NOT add a native `disabled` attribute. This is the FeasibilityBar trade documented
      at `FeasibilityBar.tsx:17-25`: a natively disabled button is not focusable, so a
      keyboard user could never reach the explanation, and the explanation is the whole
      reason for rendering the control. `SegmentedControl`'s both-attributes approach is the
      wrong precedent here — its disabled options carry no reachable explanation.
    - The click handler early-returns on the inert case, exactly as `handleStart` does:
      when `!expandable`, return without calling `change`. The early return IS the refusal.
      Never call `change` when `!expandable`.
    - After the button, and only when the control is inert and a reason was supplied, render
      a `span` with class `pane__reason`, carrying the reason id and the reason text.
    - Button first, reason second, so the chrome reads "Expand the pool — Available once the
      draft is complete" in visual and DOM order, matching the established house form.
    - Wrap the button and the reason so the branch still returns one expression.
      `SegmentedControl.tsx:83` already imports `Fragment` from preact in this codebase if
      you want the explicit form.

    **Update the two call sites.** Pool passes `POOL_EXPAND_REASON` as the new final
    argument; board passes `null`.

    **Update the `poolExpandable` prop doc comment (lines 47-52).** It currently says "the
    pool pane's expand button is not rendered", which this change makes false. Replace it
    with: the button is always rendered, and `false` makes it inert — `aria-disabled`, a
    visible reason, and a refused click — because pool-full would put the board behind a
    toggle and fail criterion 5.

    **Update the header doc block.** Beside the existing "What an expand button changes is
    the RATIO, never the membership" paragraph, record that the AVAILABILITY of an expand is
    now carried by the control's state rather than by its presence, and why: a control that
    appears and disappears is worse on a shared screen than one that is predictably inert
    (02-UI-SPEC section 8, item 3). Note that the component still holds no opinion about
    which states are available — the `expandable` prop remains the sole authority on that,
    and the parent remains the only place the rule is decided.

    ## src/ui/components/SplitPanes.css

    **`.pane__chrome` (lines 71-75)** gains three declarations:

    - `min-height: var(--target-min);` — the alignment fix. An expanded pane's chrome holds
      no control and collapsed to zero height, so its scroll track started one target-height
      above the other pane's. Reserving the height makes both panes' content start on the
      same line in every combination of states.
    - `align-items: center;` — so the button and its reason sit on one row and the chrome
      stays exactly one target tall.
    - `gap: var(--space-2);` — separation between the button and its reason.

    Comment the rule to say the `min-height` is load-bearing rather than cosmetic: it is what
    keeps the panes aligned when one chrome is empty, and that misalignment is what UAT test
    9 reported as a failed render.

    **Add the inert echo,** mirroring `FeasibilityBar.css:35-44` including its reasoning:

    - Selector `.pane__button[aria-disabled='true']`, with `opacity: 0.45;` and
      `cursor: not-allowed;`.

    Comment it as the visual echo of a state that `aria-disabled` already carries — colour is
    never the only signal, and the reason beside the button is what actually says why.

    **Add the reason element:**

    - `.pane__reason` with `font: var(--text-label);` and `color: var(--color-text-muted);`.
    - `.pane__reason::before` with content set to an em dash followed by a space.

    The em dash is generated content rather than part of the string for ONE reason, and the
    comment must give that reason and no other: the constant has to stay byte-identical to
    the entry in 02-UI-SPEC's Phase 2 string table and to the exact-equality assertion in the
    test section below, and a separator glyph is presentation, not copy.

    Do NOT write, in that comment or anywhere else, that keeping the dash out of the DOM
    keeps it out of what a screen reader announces. That is false. Per the accessible name
    and description computation as implemented by current Chrome and Firefox, textual
    `::before`/`::after` content IS included in the computed description, so the dash will
    most likely be announced wherever it sits. That is accepted here as cosmetic — a
    redundant separator, not a broken control — and neither the happy-dom test nor plan
    02-10's checklist can observe it. Record it as accepted, not as avoided, so a later
    reader does not inherit the wrong fact. If you would rather drop the `::before` than
    carry an announced dash, that is an acceptable alternative; what is not acceptable is
    keeping it under a false justification.

    No raw hex and no raw px: `--target-min`, `--space-2`, `--text-label` and
    `--color-text-muted` all exist in `src/ui/tokens.css`. The `0.45` opacity is neither a
    length nor a colour, and matches the existing FeasibilityBar and FilterBar inert
    treatment exactly.

    Do NOT declare any of the five containing-block-creating properties this file's header
    prohibits on `.draft-panes`, `.pane` or `.pane__scroll`. `min-height` is not one of them.

    ## tests/ui/draft-panes.test.tsx

    `// @vitest-environment happy-dom` is already the first line of this file. Keep it there.

    ### The amendment, and why it is not a weakened assertion

    The test at line 251 is named "offers only the board expand while a draft is running" and
    its second assertion is that the button named `Expand the pool` is undefined. The change
    above makes that button exist, so the assertion must change. Record the following in a
    comment above the replacement, because a reviewer who sees a `toBeUndefined()` deleted is
    entitled to suspect the pin was loosened to fit the code:

    The old assertion proved the pool could not be expanded mid-draft BY OMISSION — the
    button was not in the document, so no host could press it. The replacement proves the
    same invariant BY EXERCISE: the button is in the document, the host can press it, and the
    pane does not change when they do. That is strictly stronger. Absence was only ever a
    proxy for unreachability, and it was a proxy that could not distinguish "refused" from
    "not built" — which is exactly the confusion the host hit in UAT test 9.

    ### Replace that one test with two

    **First, the affordance.** Rename it to something like "renders the pool expand inert,
    with its reason, while a draft is running". After `await reachDraft()`, assert:

    - `buttonNamed('Expand the draft board')` is defined and has NO `aria-disabled`
      attribute — the board's control is unaffected by this change.
    - `buttonNamed('Expand the pool')` is defined.
    - Its `aria-disabled` attribute is exactly `'true'`.
    - `hasAttribute('disabled')` is `false`. Comment this one: focusable on purpose, because
      a native `disabled` would put the reason out of reach of the keyboard.
    - Its `aria-describedby` resolves to an element whose trimmed `textContent` is exactly
      `Available once the draft is complete`. Read the id off the attribute and
      `host.querySelector` it, rather than hard-coding a selector, so the ASSOCIATION is what
      is under test and not merely the presence of some span.

    Assert exact string equality on the reason, never `includes` — CLAUDE.md's export
    convention, and the same discipline applies to any contract string.

    **Second, the refusal.** A new test named for the invariant rather than the widget, e.g.
    "refuses the pool expand mid-draft, so the board cannot be hidden". After
    `await reachDraft()`, click `buttonNamed('Expand the pool')` and assert all four:

    - `panesRoot()?.getAttribute('data-pane')` is still `'split'`.
    - `liveRegionText()` is not `'Pool expanded to full width.'`. Assert "not that message"
      rather than emptiness — reaching the draft makes `TurnBanner` announce the turn, and
      the existing test at line 356 already establishes that convention and its reason.
    - The stored preference did not become `'pool'`: read `VIEW_KEY` out of `localStorage`,
      `JSON.parse` it, and assert `pane` is not `'pool'`. This is the assertion that proves
      the refusal happened BEFORE `onPaneChange`, not after.
    - `host.querySelector('.board')` is not null — criterion 5, asserted directly on the
      surface the constraint is actually about.

    ### Add one test for the completed case

    Assert the control becomes real once the draft is over. After
    `await reachDraft({ picks: 12 })` — 2 players x 6 rounds, the same count the existing
    test at line 383 uses to reach completion:

    - `buttonNamed('Expand the pool')` has no `aria-disabled` attribute.
    - `host.querySelector('.pane__reason')` is null — the reason is gone, not merely hidden.
    - Clicking it sets `data-pane` to `'pool'`.

    ### Leave these alone

    - The chrome button count at lines 296-297 (`toHaveLength(1)`) still holds and must keep
      holding: a collapsed strip renders the restore button and nothing else. Do not relax
      it. If it fails, the inert control landed in the collapsed branch by mistake — fix the
      component, not the assertion.
    - The coercion test at line 356 and the completed-state test at line 383. Both are
      T-02-24 evidence, named in `02-SECURITY.md:89`.

    ### What this file still cannot prove

    The header block already states that happy-dom performs no layout. The chrome
    `min-height` added above is therefore invisible here — nothing in this file can assert
    that the two panes' content starts on the same line, and nothing here can observe whether
    the CSS-generated em dash reaches the accessible description. Add both to the header's
    existing list of things this file cannot prove, rather than writing tests that appear to
    cover them. Plan 02-10 owns the alignment check; the dash is accepted as cosmetic.
  </action>

  <verify>
    <automated>npx vitest run tests/ui/draft-panes.test.tsx && npm run verify</automated>
  </verify>

  <done>
    The scoped run passes and then `npm run verify` passes — the full suite is green, with no
    intermediate state in which it is knowingly red. Mid-draft the pool chrome holds a
    focusable `Expand the pool` button with `aria-disabled="true"`, no native `disabled`, and
    a reason element it describes itself by; clicking it changes nothing. The pool expand is
    pinned as present, inert, self-explaining and refused mid-draft, and as working once
    complete. No pre-existing assertion was removed except the single `toBeUndefined()` line,
    whose replacement is argued in a comment. `src/app.tsx` is unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the spec it contradicts and the two UAT tests</name>
  <files>.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md, .planning/phases/02-host-configured-draft-night/02-UAT.md</files>

  <action>
    Locate each edit by its quoted anchor text, not by line number — numbers shift as you
    apply edits above them. The numbers below are locators only.

    ## 02-UI-SPEC.md — four edits

    This document is internally inconsistent about this exact control, and the point of these
    edits is to remove the inconsistency rather than to paper over it. It mandates the silent
    omission at lines 116 and 731, and forbids precisely that pattern at lines 787-790
    ("always rendered — a control that appears and disappears is worse on a shared screen
    than one that is predictably inert") and lines 631-633 (Blind and Snake rendered disabled
    with a visible "Not yet available"). The host resolved it toward the inert control.

    **Edit 1 — the availability table, row "Draft screen, draft in progress" (line 116).**
    This row is the SOURCE of the rule and the actual defect location; Edit 3 below is only
    its echo, and doing one is not doing the other — their replacement texts share no
    wording. That cell currently ends with "The pool pane's expand button is
    **not rendered.**"

    Replace the cell, keeping the row on one physical line, with wording that contains the
    phrase `always rendered, and is inert here` verbatim — the verify gate matches on it.
    Target text: "`split` and `board-full` only. The pool pane's expand button is **always
    rendered, and is inert here** — `aria-disabled="true"`, no native `disabled` so it stays
    focusable, with the visible reason `Available once the draft is complete` beside it. It
    is never removed from the document."

    The words "not rendered" must not survive anywhere in that row; the gate reads the row
    with a line-anchored match and asserts their absence within it. The config-screen row
    directly above legitimately keeps its own "the expand control is not rendered" — that row
    is out of scope here and the gate does not look at it.

    **Edit 2 — a resolution note.** Immediately after the paragraph ending "The planner
    should read it as such." (lines 123-124), add a short subsection recording the amendment.
    It must contain the literal phrase `Amended by plan 02-09` — the gate matches on it.
    Content:

    - What changed: the availability scoping is unchanged; only the way it is COMMUNICATED
      changed, from omission to an inert control with a reason.
    - Why: as written, this section contradicted this document's own convention in section 8
      item 3 and its "Not yet available" treatment of the ban modes. The pool expand was the
      single control in the phase handled by omission.
    - Evidence: UAT test 9 — the host read the empty chrome slot as a broken render and
      reported it as a bug.
    - What did NOT change: `poolExpandable = complete` in `src/app.tsx`, and both T-02-24
      coercions. pool-full remains unreachable while a draft is running.
    - `Amended by plan 02-09`, closing gap 1 in `02-UAT.md`.

    You may quote or describe the superseded wording in this note if it helps a reader. The
    gate's absence check is scoped to the Edit 1 table row alone, so prose anywhere else in
    the document cannot trip it.

    **Edit 3 — section 6, Draft screen pool pane, Row 1 (line 731).** Replace the
    parenthetical "**not rendered while a draft is in progress**" with "**rendered inert
    while a draft is in progress**, with its reason beside it". Keep the existing
    cross-reference to the tension section that follows it.

    **Edit 4 — the Phase 2 string table.** Directly after the "Expand pool" row (line 986),
    add a row whose Where column is `Expand pool, unavailable` and whose String column is the
    reason `Available once the draft is complete`, noting that the em dash separator is
    CSS-generated and not part of the string.

    Do not touch any other section of this document.

    ## 02-UAT.md — two tests, and the counts

    **Test 9 (lines 48-52).** Its `expected:` carries D-18/D-19's unscoped wording, "You can
    switch which pane is expanded", which `02-UI-SPEC.md:116` explicitly declined. It is
    stale, and it is why the host tested for a capability the design had already scoped out.
    Replace that final sentence with wording to this effect: you can expand the draft board
    and restore it, and that choice survives a page reload; the pool's own expand control is
    visible but inert while the draft runs, with the reason `Available once the draft is
    complete` beside it, and it becomes usable once the draft is over. Leave the rest of the
    `expected:` text as it stands.

    Set `result: pending`. Remove the `reported:` and `severity:` lines — they belong to the
    superseded run — and add a `note:` recording that the truth statement was corrected and
    the affordance built under plan 02-09, and that the test awaits re-verification.

    **Test 16 (lines 90-95).** Add a `setup:` block before `expected:`. The test has none,
    and therefore inherited a zero-ban tournament from test 14, which is the entire defect.
    The setup: from the landing screen choose `New tournament`, add two players, ban two
    species by name in the Bans group, confirm the group reads `2 bans`, then `Start draft`.

    Rewrite `expected:` to assert both cases:

    - The draft screen's top bar shows `Bans (2)`. Expanding it lists exactly the two species
      you banned, by name, and it contains no buttons — there is no way to add or remove a
      ban once the draft has started.
    - Second, the zero-ban case: a tournament started with no bans shows no disclosure at all
      — not `Bans (0)`, nothing. Absence is the specified behaviour there
      (`02-UI-SPEC.md:1013`), not a missing render. This is what the host actually observed,
      and it was correct.

    Set `result: pending`. Remove `reported:` and `severity:`. Add a `note:` recording that
    no code change was warranted — `TopBar.tsx:209-218` matches `02-UI-SPEC.md:1013`, and its
    four tests at `tests/ui/ban-mode.test.tsx:506-548` pass — and that the defect was the
    test's missing setup.

    **Summary block (lines 99-104).** Update to: total 16, passed 14, issues 0, pending 2,
    skipped 0, blocked 0.

    **Frontmatter `updated:`** — set to today's date.

    **Do NOT touch** the frontmatter `status:` field or the `## Gaps` section. Both belong to
    the re-verification run that closes these gaps; editing them here would mark closed a
    thing no human has re-tested.
  </action>

  <verify>
    <automated>npm run verify && node -e "const fs=require('fs');const d='.planning/phases/02-host-configured-draft-night/';const spec=fs.readFileSync(d+'02-UI-SPEC.md','utf8');const uat=fs.readFileSync(d+'02-UAT.md','utf8');const row=(spec.match(/^\| Draft screen, draft in progress \|.*$/m)||[''])[0];const checks=[['spec availability row found',row.length>0],['spec availability row now inert',/always rendered, and is inert here/.test(row)],['spec availability row omission gone',row.length>0&&!/not rendered/.test(row)],['spec resolution note',/Amended by plan 02-09/.test(spec)],['spec inert row',/rendered inert while a draft is in progress/.test(spec)],['spec reason string',/Available once the draft is complete/.test(spec)],['spec unavailable row',/Expand pool, unavailable/.test(spec)],['uat two pending',(uat.match(/^result: pending$/gm)||[]).length===2],['uat test16 setup',/^setup:/m.test(uat)],['uat counts',/issues: 0/.test(uat)&&/pending: 2/.test(uat)],['uat gaps untouched',/^## Gaps$/m.test(uat)]];let bad=checks.filter(c=>!c[1]);if(bad.length){console.error('FAIL: '+bad.map(c=>c[0]).join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>

  <done>
    `npm run verify` passes and the node gate prints OK — including the four checks that pin
    Edit 1 and Edit 2 specifically: the availability row exists, now says the control is
    always rendered and inert, no longer contains "not rendered", and the resolution note is
    present. The spec no longer prescribes the silent omission at either of its two sites,
    records why it changed and what did not, and carries the reason string in its copy table.
    Both UAT tests are re-runnable as written and the counts agree with the results.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` view-prefs to `SplitPanes` | An attacker-controlled or hand-edited `pane` value crosses into layout state. This is the boundary T-02-24 governs, and the only one this plan's code touches. |
| Host pointer/keyboard to the pool expand control | This plan puts a NEW pressable control on the draft screen where none existed. Its whole purpose is that pressing it must do nothing mid-draft. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24 | Tampering | view-prefs `pane` to `SplitPanes` | mitigate (already closed; must stay closed) | Unchanged and untouched: union check at `view-prefs.ts:94`, second independent coercion at `app.tsx:519`, both files absent from `files_modified`. Task 1 preserves the pinning tests at `draft-panes.test.tsx:356` and `:383` verbatim. |
| T-02-09-01 | Tampering | new inert `Expand the pool` button in `SplitPanes.tsx` | mitigate | The click handler early-returns when `!expandable`, so `onPaneChange` is never called and nothing reaches `saveViewPrefs`. Refusal is asserted behaviourally in Task 1's test section across all four observable channels: rendered `data-pane`, live region, `localStorage`, and the board's continued presence in the DOM. |
| T-02-09-02 | Denial of Service | ROADMAP criterion 5, board visibility | mitigate | A functional pool expand would collapse the board to a 44px strip on the shared screen. Prevented at three independent layers, none of which this plan alters: `poolExpandable = complete`, the `app.tsx:519` coercion, and the handler's early return. |
| T-02-09-03 | Information Disclosure | the new reason string | accept | `Available once the draft is complete` is a static UI constant. It carries no tournament data, no player names and no host input, and is not interpolated. |
| T-02-09-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no packages. Runtime dependencies stay exactly `preact` and `@preact/signals`; `package.json` is not in `files_modified`. No legitimacy audit is required. |
</threat_model>

<verification>
- `npm run verify` passes — this is the repo's single gate: `check:pure`, `check:nohtml`,
  `test`, `build`.
- `src/app.tsx` and `src/adapters/view-prefs.ts` show no diff against the base commit.
- `package.json` shows no diff.
- No raw hex and no raw px enter `SplitPanes.css`; every new value is a token from
  `src/ui/tokens.css`.
- What is NOT verified here: that the two panes' content visually aligns, and that the inert
  control reads as unavailable rather than broken on a real screen. happy-dom computes no
  layout, which the test file's own header already states. Plan 02-10 owns both.
- Also not verified, and accepted rather than fixed: whether the CSS-generated em dash is
  announced as part of the control's accessible description. It most likely is. Cosmetic.
</verification>

<success_criteria>
- Mid-draft, the pool pane shows a focusable `Expand the pool` button carrying
  `aria-disabled="true"` and no native `disabled`, described by a visible
  `Available once the draft is complete`.
- Pressing it mid-draft leaves `data-pane` at `split`, announces nothing, writes nothing, and
  leaves `.board` in the document.
- Once the draft is complete the button carries no `aria-disabled`, no `.pane__reason` exists,
  and pressing it sets `data-pane` to `pool`.
- `.pane__chrome` reserves `min-height: var(--target-min)`.
- The full suite is green at every commit boundary — the component change and the test that
  pins it land together.
- `02-UI-SPEC.md` no longer prescribes the silent omission at either of its two sites, and
  records why it changed and what did not.
- `02-UAT.md` tests 9 and 16 are re-runnable as written, both `pending`, counts consistent.
</success_criteria>

<output>
Create `.planning/phases/02-host-configured-draft-night/02-09-SUMMARY.md` when done.

Record explicitly: the exact reason string shipped, the amended test names in
`draft-panes.test.tsx` with the one-line argument for why the pin is stronger rather than
weaker, and confirmation that `src/app.tsx` and `src/adapters/view-prefs.ts` were not
touched.
</output>
</content>
