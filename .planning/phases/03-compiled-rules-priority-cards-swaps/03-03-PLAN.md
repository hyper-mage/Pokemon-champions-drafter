---
phase: 03-compiled-rules-priority-cards-swaps
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/ui/components/SchedulePreview.tsx
  - src/ui/components/SchedulePreview.css
  - src/ui/screens/ConfigScreen.tsx
  - src/ui/screens/ConfigScreen.css
  - tests/ui/schedule-reorder.test.tsx
  - tests/ui/config-screen.test.tsx
autonomous: true
# RULE-01's surface is the `Megas required per team` field plus its amended helper — the host
# states a composition requirement and the tool shows the rule set it becomes. RULE-06's surface is
# the per-row Move up / Move down control. The compiler itself is 03-02.
requirements: [RULE-01, RULE-06]

must_haves:
  truths:
    - "The host states a composition requirement at config time and sees the rule set it compiles to, before starting anything (RULE-01, D-02)"
    - "The host reorders the derived schedule with per-row buttons and the draft runs the order they left it in (RULE-06, D-13, D-14)"
    - "A reorder that would change nothing is inert with a stated reason rather than missing, and sheds its inert ARIA the moment the move becomes possible"
    - "The reorder dispatches nothing — it is config-time form state resolved into the single schedule/compiled action at Start (D-13)"
    - "The round count stays 6 and no derivation reads a literal — `ROUNDS` is one constant in one place (D-06)"
    - "A host who wants a Mega-less night learns from the screen that 0 means no slot is a Mega slot and nothing exports with a stone"
  artifacts:
    - path: "src/ui/components/SchedulePreview.tsx"
      provides: "The ordered schedule rows, the reorder buttons, the nothing-to-reorder state"
      exports: ["SchedulePreview"]
    - path: "src/ui/components/SchedulePreview.css"
      provides: "Row styling from tokens only"
    - path: "src/ui/screens/ConfigScreen.tsx"
      provides: "The Round schedule sub-section, the reorder state, and handleStart passing the reordered schedule"
      contains: "Round schedule"
  key_links:
    - from: "src/ui/screens/ConfigScreen.tsx"
      to: "src/core/compile.ts"
      via: "compile(rules, ROUNDS) seeds the preview state"
      pattern: "compile\\("
    - from: "src/ui/screens/ConfigScreen.tsx"
      to: "src/store.ts"
      via: "createTournament receives the reordered schedule, not a recomputed one"
      pattern: "schedule:"
---

## Phase Goal

Verbatim from `.planning/ROADMAP.md` §Phase 3 — this Goal line is not in `As a … I want to … so
that …` form and was not rewritten here; run `/gsd mvp-phase 3` if the three-slot form is wanted.

> The draft becomes the one the spec describes — composition requirements compile into a round
> schedule that types the team slots, players bid priority cards for turn order over that visible
> schedule, and swaps can only take something the target slot allows.

**Slice delivered by this plan:** the host types `2` into `Megas required per team`, sees six rows
appear reading `Round 1 — Mega` down to `Round 6 — Open`, walks the Mega rounds to the bottom with
`Move down`, starts the draft, and the board headers show Mega on rounds 5 and 6.

<objective>
Show the host the schedule their requirement compiles to, and let them reorder it.

Purpose: RULE-06 is the reason the schedule is materialized into the log at all — a document that
recorded only the rules would recompute the canonical order on load and lose the reorder silently.
This plan is where the host's permutation is authored, and 03-02's `schedule/compiled` is where it
survives.

Output: `SchedulePreview`, the `Round schedule` sub-section inside the `Mega rules` group, the
amended `Megas required per team` helper, and a corrected `ROUNDS` comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-CONTEXT.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-RESEARCH.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-PATTERNS.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Contract this plan creates. -->

```tsx
// src/ui/components/SchedulePreview.tsx
export interface SchedulePreviewProps {
  /** The current schedule, already compiled and possibly reordered. Length is config.rounds. */
  schedule: readonly RoundSpec[];
  /** Emitted with the index the host wants moved and the direction. The parent owns the array. */
  onMove: (index: number, direction: 'up' | 'down') => void;
}

export function SchedulePreview(props: SchedulePreviewProps): VNode;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SchedulePreview — six rows, two buttons each, and the reasons they are inert</name>
  <files>src/ui/components/SchedulePreview.tsx, src/ui/components/SchedulePreview.css, tests/ui/schedule-reorder.test.tsx</files>

  <read_first>
    - `src/ui/components/PlayerList.tsx` (whole file) — PATTERNS calls this an **exact** analog.
      `:11-22` is the stateless-with-callbacks doc block and the "the rows are not a list element,
      and that is on purpose / the numbered order IS an `<ol>` because there the ordinal is the
      information" decision. `:40-50` (`rowLabel`) and `:89-95` are the per-row button whose
      accessible name **contains** its visible label — SC 2.5.3. `:118-130` is the `<ol>`.
    - `src/ui/components/SplitPanes.tsx:101` (`PaneAvailability`) and `:265-331` — PATTERNS **S-5**:
      one vnode shape across the boundary, `aria-disabled` rather than native `disabled`,
      `undefined` rather than `'false'` so the ARIA is **shed** when the condition lifts (WR-04),
      and the handler returning early or the attribute lies (`FilterBar.tsx:213-219`).
    - `src/ui/components/BanChipList.tsx` + `BanChipList.css` (whole files) — PATTERNS **S-4**: the
      component/stylesheet pairing, import order with `./X.css` last, tokens only, no `:focus` rule,
      motion budget colour-only ≤ 120ms, `key` by id.
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md` §2 in full — the row
      copy, the five accessible-name cases, the inert rule, the single visible rule line, the
      nothing-to-reorder states, and the focus-follows-the-moved-round contract.
    - `03-UI-SPEC.md` §Interaction & Accessibility — "No roving tabindex on either new surface"
      (stated explicitly so `use-roving-tabindex` is not wired out of habit) and the
      announce-vs-focus rule.
    - `tests/ui/config-screen.test.tsx` — the analog for the new test file's setup;
      `// @vitest-environment happy-dom` on the **first** line, `announce` reset in `beforeEach`.
  </read_first>

  <behavior>
    - Six rows render as `<li>`s inside one `<ol>`, reading `Round 1 — Mega` … `Round 6 — Open`.
    - Each row carries two buttons with the visible labels `Move up` and `Move down`, both at
      `--target-min` in both axes.
    - Accessible names, exactly: `Move up to round {n−1}` / `Move down to round {n+1}` when the move
      changes the schedule; `Move up — round 1 is already first`; `Move down — round {rounds} is
      already last`; `Move up — round {n−1} is already a Mega round` and
      `Move down — round {n+1} is already an open round` when the neighbour has the same kind.
    - Every accessible name **contains** its visible label as a substring.
    - An inert button carries `aria-disabled="true"`, stays focusable, and its click handler emits
      no `onMove`. When the same button becomes movable the attribute is `undefined`, not
      `'false'`.
    - One visible rule line under the list, at `--text-label` `--color-text-muted`:
      `A move that would not change the schedule is unavailable.` It renders only when at least one
      button is inert.
    - When every round has the same kind, **no buttons render at all** and the list is followed by
      `Every round is open, so there is nothing to reorder.` or `Every round is a Mega round, so
      there is nothing to reorder.`
    - After a successful move, focus lands on the same-named button in the row the kind moved to.
  </behavior>

  <action>
Create `src/ui/components/SchedulePreview.tsx` and its sibling stylesheet.

The component owns no state: `schedule` and `onMove` arrive as props and every change goes back out
as a callback, which is what keeps `ConfigScreen` the one place that knows the config screen is
pre-document form state. Copy that paragraph's substance from `PlayerList.tsx:11-22` into this
file's doc block, and add the second half specific to this surface: the rows **are** an `<ol>`,
because here the ordinal is the information — that is the same test `PlayerList` applies to decide
its own two lists differently.

Row text is `Round {n} — {Mega|Open}` at `--text-body`, using D-14's own wording. Rows take
`--color-surface` fill, `--radius`, `--space-3` padding, `--space-2` between them.

Both buttons render in **one vnode shape** whether movable or inert — a bare button in one branch
and a Fragment in the other unmounts the node and drops focus to `<body>`, which is exactly the
regression plan 02-11 fixed. Compute the button's state as a small discriminated union in the
`PaneAvailability` shape (`{ movable: true } | { movable: false; reason: string }`) so
"unavailable with no reason given" is unrepresentable, then derive the accessible name from it.
The handler returns early when inert, before calling `onMove`, or the attribute lies.

Per-row visible reasons were rejected by 03-UI-SPEC §2 — twelve reason lines on a config form is
noise — so the rule is stated once beneath the list. Do not repeat plan 02-09's mistake in the
other direction: nothing is **omitted** here, the buttons are present and explained.

When `schedule.every((spec) => spec.kind === schedule[0].kind)` the buttons are not rendered at
all, following the shipped precedent for a wholly void affordance (`Clear the banlist` is not
rendered when nothing is banned, `ConfigScreen.tsx:868-881`), and the matching sentence renders
instead. Choose the sentence from the kind, never from `megasRequiredPerTeam` — the component reads
the schedule it is handed.

Focus after a move: the parent re-renders with a new array, so the component moves focus to the
same-named button of the row the moved kind now occupies. Do this from the click handler with a ref
map keyed by round index, and leave a comment recording why: leaving focus on the pressed button
makes a second press reverse the first, which makes walking a Mega round from position 6 to
position 1 impossible by keyboard.

**No roving tabindex.** 03-UI-SPEC states this explicitly for this surface — twelve buttons is not
the large uniform set `use-roving-tabindex` exists to collapse, and `PlayerList` already sets the
plain-tab-stops precedent for a form list of rows.

Stylesheet per PATTERNS **S-4**: tokens only, no raw hex, no raw px where a token covers it, no
`:focus` rule, and an inert button at `opacity: 0.45`.

Create `tests/ui/schedule-reorder.test.tsx` with `// @vitest-environment happy-dom` as the **first**
line, covering every `<behavior>` bullet. Include an explicit assertion that a button's
`aria-disabled` attribute is **absent** (not `'false'`) after a re-render that makes the move
possible — that is WR-04 and it is the assertion that catches the regression.
  </action>

  <verify>
    <automated>npx vitest run tests/ui/schedule-reorder.test.tsx && npm run check:nohtml</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/ui/schedule-reorder.test.tsx` exits 0.
    - `npm run check:nohtml` exits 0.
    - `grep -c "export function SchedulePreview" src/ui/components/SchedulePreview.tsx` returns 1.
    - `grep -c "aria-disabled" src/ui/components/SchedulePreview.tsx` returns at least 1 and
      `grep -c "disabled={" src/ui/components/SchedulePreview.tsx` returns 0 — never native
      `disabled`.
    - `grep -c "use-roving-tabindex" src/ui/components/SchedulePreview.tsx` returns 0.
    - A test asserts the six row texts in document order for a `count: 2` schedule.
    - A test asserts the accessible name of row 1's `Move up` is
      `Move up — round 1 is already first` and that it contains the substring `Move up`.
    - A test asserts clicking an inert button emits no `onMove` call.
    - A test asserts `getAttribute('aria-disabled')` is `null` once the button becomes movable.
    - A test asserts that with an all-open schedule no `Move up` button exists anywhere and the
      text `Every round is open, so there is nothing to reorder.` is present.
    - A test asserts focus after a successful move is on the same-named button of the destination
      row.
    - `grep -Ec "#[0-9a-fA-F]{3,6}" src/ui/components/SchedulePreview.css` returns 0.
  </acceptance_criteria>

  <done>`SchedulePreview` renders the compiled schedule as an ordered list with keyboard-operable reorder controls that are inert with a stated reason when a move would change nothing, and shed that state when it would not.</done>
</task>

<task type="auto">
  <name>Task 2: The Round schedule sub-section, and the rule set the host is actually stating</name>
  <files>src/ui/screens/ConfigScreen.tsx, src/ui/screens/ConfigScreen.css, tests/ui/config-screen.test.tsx</files>

  <read_first>
    - `src/ui/screens/ConfigScreen.tsx` — the file being modified. `:51-82` (the module doc block,
      the group-order paragraph, and **the stale `ROUNDS` comment at `:77-82`**), `:359-371`
      (`dualMegaRows`, the derive-rows-from-roster pattern), `:373-457` (the ban write path —
      idempotent single write, computation-local `Set`, `announce` on change), `:684-745`
      (`handleStart`, including the `schedule:` line 03-02 left for this plan), `:840-898` (the
      `Bans` fieldset as the group template).
    - `src/ui/components/NumericField.tsx:22-29, 52-60` — `min`/`max` are affordances, the caller
      holds raw string and parsed `number | null`.
    - `src/ui/components/LiveRegion.tsx` — `announce` is a module-level signal that outlives a
      render.
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md` §1 (the six-group
      order and the sub-section layout inside `Mega rules`), §2 (the sub-heading, the helper line),
      §Copywriting Contract → Config screen (**the amended `Megas required helper` string, verbatim**),
      and §Live-region announcements (`Round {n} is now a Mega round. Round {m} is now open.`).
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-CONTEXT.md` D-06 and
      §Specific Ideas item 2 — the three comments this phase makes false.
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-RESEARCH.md` §Open Questions 1
      and 3 — `Re-roll pool` stays unchanged, and the preview sits below the Megas-per-team field
      because the schedule is that field's visible consequence.
  </read_first>

  <behavior>
    - A `Round schedule` sub-heading renders inside the `Mega rules` group, directly below
      `Megas required per team` and above the dual-Mega rows.
    - Changing `Megas required per team` from 2 to 3 re-seeds the preview from
      `compile(rules, ROUNDS)`, discarding any prior reorder — the requirement is the source and the
      permutation is applied to what it produces.
    - Reordering, then starting, produces a document whose `schedule/compiled` carries the
      reordered kinds — not the canonical ones.
    - `handleStart` passes the preview state as `schedule`, and `rules` in the config literal is
      `[{ kind: 'mega', count: megasRequiredPerTeam ?? 0 }]`.
    - The `Megas required per team` helper reads, verbatim: `0 means no Mega requirement, and no
      slot is a Mega slot — nothing exports with a Mega Stone. A requirement of {n} makes {n} rounds
      Mega-only and needs at least {x} Pokémon that can still Mega.`
    - A successful move announces `Round {n} is now a Mega round. Round {m} is now open.`
    - Nothing on this screen dispatches. `git grep dispatch src/ui/screens/ConfigScreen.tsx`
      finds nothing new.
    - `Re-roll pool` is unchanged and still available.
  </behavior>

  <action>
Add the `Round schedule` sub-section to the `Mega rules` fieldset, positioned directly beneath the
`Megas required per team` field per 03-UI-SPEC §1 — the schedule is that field's visible
consequence. Sub-sections are `--text-heading` headings separated by `--space-5`, which is the gap
the form already uses between groups, one level in.

Hold the schedule as component state seeded from `compile([{ kind: 'mega', count: megasRequiredPerTeam ?? 0 }], ROUNDS)`
and re-seeded whenever `megasRequiredPerTeam` changes. Implement `onMove(index, direction)` as a
swap of two adjacent entries followed by a re-index so `spec.index` is contiguous from 1 again —
the index is the round number, not the kind's identity, so a move swaps **kinds** between fixed
round numbers rather than moving a numbered row. Getting this backwards produces rows reading
`Round 3` above `Round 2`, which is the visible symptom to test for.

Helper line beneath the sub-heading, verbatim from 03-UI-SPEC §2: `The draft runs these rounds in
this order. Reorder them before you start; the schedule is fixed once the draft begins.`

Amend the `Megas required per team` helper to the 03-UI-SPEC string quoted in `<behavior>`. Its
first clause is the answer to the most likely host confusion in the phase (03-RESEARCH stress-test
case 3): a host who wants a Mega-less night otherwise reaches for 76 forme bans instead of typing
`0`. `{x}` is `players × count`.

Announce on a successful move using the module-level `announce`, with the 03-UI-SPEC string. This
surface satisfies the announce-vs-focus rule the hard way and deliberately: the destination is also
carried by the newly focused button's own accessible name, so nothing here exists only in the
announcement.

In `handleStart`, replace 03-02's temporary `schedule: compile(...)` call with the preview state,
and add it to the `useCallback` dependency array. This is the whole of D-13: the reorder is
config-time form state, resolved into one `schedule/compiled` action at Start, exactly as the
banlist resolves into `pool/built`. Nothing here dispatches, and there is no `schedule/reordered`
action to reach for.

**Correct the stale `ROUNDS` comment at `:77-82` in this same change.** It currently predicts that
"Phase 3 makes the round count a host decision"; D-06 declines that — DRFT-04 says six, and a
shorter team exports a paste Showdown accepts but a Champions match does not. Rewrite it to say
what carries forward: `ROUNDS` is one constant in one place rather than a `6` scattered through the
derivations that read it, the card count and the schedule length and the slot-array length all read
`config.rounds`, and host-selectable team size is recorded as deferred rather than forgotten.
Update the module doc block's "Group order" paragraph to name the sub-sections inside `Mega rules`.

Extend `tests/ui/config-screen.test.tsx` per `<behavior>`, including the re-seed-on-requirement-
change case and an assertion on the created document's schedule after a reorder.
  </action>

  <verify>
    <automated>npx vitest run tests/ui/config-screen.test.tsx tests/ui/schedule-reorder.test.tsx && npm run check:nohtml</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/ui/` exits 0.
    - `npm run check:nohtml` exits 0.
    - `grep -c "Round schedule" src/ui/screens/ConfigScreen.tsx` returns at least 1.
    - The string `The draft runs these rounds in this order. Reorder them before you start; the schedule is fixed once the draft begins.`
      appears verbatim in `src/ui/screens/ConfigScreen.tsx`.
    - The string `0 means no Mega requirement, and no slot is a Mega slot` appears verbatim.
    - `grep -c "Phase 3 makes the round count a host decision" src/ui/screens/ConfigScreen.tsx`
      returns 0 — the stale comment is gone.
    - `grep -c "const ROUNDS = 6" src/ui/screens/ConfigScreen.tsx` returns 1 — the constant stays.
    - `grep -c "dispatch" src/ui/screens/ConfigScreen.tsx` returns 0.
    - A test starts a draft after moving the Mega rounds to the bottom and asserts the created
      document's `schedule/compiled` payload kinds deep-equal
      `['open','open','open','open','mega','mega']`.
    - A test asserts changing `Megas required per team` from 2 to 3 re-seeds the preview to three
      leading Mega rounds.
    - A test asserts row texts stay in ascending round order after a move.
  </acceptance_criteria>

  <done>The host states a Mega requirement, sees the six-round schedule it compiles to, reorders it with keyboard-operable buttons, and starts a draft that runs the order they left — with nothing dispatched before Start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-typed requirement → the preview state | A free numeric field feeds `compile`, which must stay total for every value the field can hold, including `null` and a value above `ROUNDS`. |
| preview state → `createTournament` | The permutation the host sees is the one that must reach the log. Recomputing at Start would silently discard it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | `ConfigScreen` reorder state | mitigate | The reordered schedule is passed to `createTournament` as a result, never recomputed at Start. Asserted by a test that reorders and then reads the created document's payload — the one assertion a recompute-on-start implementation fails. |
| T-03-11 | Denial of Service | `compile` via the numeric field | mitigate | `compile` is total for every `number \| null` the field can hold (03-02 acceptance criteria); a requirement above `ROUNDS` lays out an all-Mega schedule and is blocked by `megasExceedRounds`, never thrown. |
| T-03-12 | Tampering | mid-draft reorder | accept | Not built, deliberately (D-13). A mid-draft reorder would retype slots players have already filled with no validator left to catch it. Recorded in CONTEXT `<deferred>`; the reserved marker line in every board header is what would let it land later without a layout change. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; `package.json` is not modified. No drag-and-drop library — the reorder is buttons (D-14), and STACK rejects DnD packages by name. |
</threat_model>

<verification>
- `npm run verify` exits 0.
- Reordering and starting yields a `schedule/compiled` payload matching what was on screen.
- `git diff --stat package.json` is empty.
- Keyboard-only walk: tab to `Move down` on round 1 and press it five times; the Mega round reaches
  round 6 without the pointer.
</verification>

<success_criteria>
- The full compiled schedule is on screen at config time, before any draft exists.
- The host's reorder survives Start and reaches the log.
- Inert reorder buttons state their reason and shed the ARIA when the move becomes possible.
- `ROUNDS = 6` stays, with a comment that is true.
</success_criteria>

<output>
Create `.planning/phases/03-compiled-rules-priority-cards-swaps/03-03-SUMMARY.md` when done.
</output>
