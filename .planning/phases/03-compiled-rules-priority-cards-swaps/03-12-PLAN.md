---
phase: 03-compiled-rules-priority-cards-swaps
plan: 12
type: execute
wave: 12
depends_on: ["03-11"]
files_modified: []
autonomous: false
# CARD-02 and CARD-07 are BUILT in 03-02 and 03-07. Their acceptance is not: 03-UI-SPEC makes the
# three-metre reading of the Mega markers and the hand pips a mandatory, non-substitutable physical
# check, and happy-dom performs no layout. This plan is where that acceptance is recorded, and it
# writes no source file — the same shape plans 02-10 and 02-13 took in Phase 2.
requirements: [CARD-02, CARD-07]

must_haves:
  truths:
    - "Someone standing three metres from the shared screen can read the Mega marker on a round header (CARD-02, DRFT-14 assertion 10)"
    - "Someone standing three metres away can read which cards each player still holds (CARD-07, DRFT-14 assertion 9)"
    - "A card face and its digit are legible at the same distance (DRFT-14 assertion 11)"
    - "Adding the hand strip did not make the board taller or introduce an internal scrollbar at 8 players (DRFT-14 assertion 12)"
    - "The two new focus-moving announcements — a schedule reorder and a card resolution — are heard by a real screen reader, and the outcome is recorded either way (WR-02)"
    - "The rotation and the low-plays-first rule were tried with real players before being called done (ROADMAP Notes)"
  artifacts:
    - path: ".planning/phases/03-compiled-rules-priority-cards-swaps/03-12-SUMMARY.md"
      provides: "The recorded outcome of all three checks, including the screen size tested"
  key_links: []
---

## Phase Goal

Verbatim from `.planning/ROADMAP.md` §Phase 3 — this Goal line is not in `As a … I want to … so
that …` form and was not rewritten here; run `/gsd mvp-phase 3` if the three-slot form is wanted.

> The draft becomes the one the spec describes — composition requirements compile into a round
> schedule that types the team slots, players bid priority cards for turn order over that visible
> schedule, and swaps can only take something the target slot allows.

**Slice delivered by this plan:** nothing new on screen. This is the acceptance the automated suite
structurally cannot give: happy-dom performs no layout, computes no font metrics, and cannot stand
three metres away.

<objective>
Record the physical and human checks 03-UI-SPEC declares mandatory.

Purpose: 03-UI-SPEC §DRFT-14 states its own arc-minute arithmetic as **a prediction, not a result**,
and names the pass that would falsify it. Shipping without running that pass would leave the phase's
legibility claim untested on the one surface the whole room reads at once.

Output: `03-12-SUMMARY.md` carrying the outcome per surface, the physical screen size tested, and
the screen-reader result — recorded whether it passes or fails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md
@.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md
@.planning/phases/02-host-configured-draft-night/02-UAT.md
@.planning/phases/03-compiled-rules-priority-cards-swaps/03-11-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: The three-metre pass over the card surfaces</name>

  <read_first>
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md` §DRFT-14 as a Checkable
      Assertion — extended: assertions 9 through 12, the arc-minute table, and **"The three-metre
      question, stated honestly rather than asserted"**, which predicts that every surface passes on
      a TV-sized screen and that on a 24" monitor at three metres only the card digit is comfortable
      while the pips and the marker are marginal.
    - `.planning/phases/02-host-configured-draft-night/02-UI-SPEC.md` §DRFT-14 — the original eight
      assertions and the two-pass setup this extends.
    - `.planning/phases/02-host-configured-draft-night/02-UAT.md` — how Phase 2 recorded its result.
  </read_first>

  <action>
    This task writes no code. Run `npm run dev`, build the setup described below, hand the screen to
    the host, and pause for their verdict. Do not fix anything noticed while setting up — record it
    and let the host decide. Capture their exact wording verbatim in the summary rather than
    paraphrasing it into a diagnosis, and record the screen's physical size and resolution whatever
    the outcome, because 03-UI-SPEC's prediction is stated per screen size and is falsifiable only
    against a named one.
  </action>

  <what-built>
    Board round headers carrying a `Mega` marker above the columns they type (03-02); per-player
    hand strips of six pips in the board label cells (03-07); the card panel with `--card-min`
    64px card faces and `--text-display` digits (03-08).
  </what-built>

  <how-to-verify>
    Setup, matching the Phase 2 pass exactly: 8 players, at least two rounds filled, `standard`
    density, a two-Mega schedule, and a card-play step in progress. Stand **three metres** from the
    screen.

    1. Read aloud the value on each card in the hand on the clock.
    2. Read aloud the value and the player of each card already down in the played row.
    3. Read aloud the six pips in two different board rows, and say which are struck through.
    4. Read aloud the `Mega` marker on each round header, and say which rounds are Mega rounds.
    5. Confirm the 8-player board shows all eight rows with **no internal vertical scrollbar** in
       the split pane.
    6. Record the physical screen size and resolution you tested on.

    Report the result per surface — card digit, played-row name, hand pip, Mega marker, board
    height. A failure on the pips or the marker is a finding about `--text-heading` and
    `--text-body` on the board, **not** a licence to introduce a fifth font size: the remedies
    available inside this contract are the existing scale and the existing pane states.
  </how-to-verify>

  <acceptance_criteria>
    - The summary records a pass or fail for each of the five surfaces named above.
    - The summary records the screen's physical size and resolution.
    - If any surface fails, the summary names which token the finding is about and does not propose
      a new font size.
  </acceptance_criteria>

  <resume-signal>Type "approved" or describe which surface failed and on what screen.</resume-signal>

  <done>The host has answered. Either all five surfaces read at three metres on a named screen, or their exact description of the failure and the surface it affects is recorded for a follow-up plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The screen-reader pass on four transitions</name>

  <read_first>
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-UI-SPEC.md` §Interaction &
      Accessibility — **the announce-vs-focus rule** and the row requiring the two new
      focus-moving announcements be checked in the same pass as Phase 2's two outstanding ones.
    - `src/ui/components/SplitPanes.tsx:146-167` — the open question this pass exists to answer: a
      polite announcement queued alongside a focus move is routinely preempted, and Phase 2 could
      not settle it without a real screen reader.
  </read_first>

  <action>
    This task writes no code beyond one comment per component. Run the app with a real screen reader,
    walk the four transitions below, and record what was spoken and where focus landed. Then write
    the outcome into the relevant component's doc block — `SchedulePreview`, `CardPanel` and
    `SplitPanes` — because 03-UI-SPEC requires the answer live beside the code that depends on it,
    not only in a planning file. Do not change any behaviour in response to a finding; report it.
  </action>

  <what-built>
    Four focus-moving transitions that also announce: the two pane-expand transitions carried over
    from Phase 2, the schedule reorder (03-03), and the card resolution that unmounts the card panel
    and hands focus to the pool grid (03-08).
  </what-built>

  <how-to-verify>
    With NVDA (Windows) or VoiceOver (macOS) running:

    1. On the config screen, move a Mega round down one position with the keyboard. Report what is
       spoken, and whether the newly focused button's own name (`Move down to round {n}`) is heard.
    2. Play the last card of a round. Report whether `Round {r} pick order: …` is heard, and where
       focus lands.
    3. Collapse and expand each pane. Report what is spoken.

    The design does not depend on the answer — every fact is also carried by a focused control's own
    name or by persistent on-screen text — so a preempted announcement is a **finding to record**,
    not a blocker.
  </how-to-verify>

  <acceptance_criteria>
    - The summary records, per transition, what was spoken and where focus landed.
    - The summary names the screen reader and operating system used.
    - Any preemption found is recorded in the relevant component's doc block, not only in the
      summary.
  </acceptance_criteria>

  <resume-signal>Type "approved" or describe what was preempted.</resume-signal>

  <done>All four transitions have a recorded spoken result and a recorded focus destination, named against a specific screen reader and OS, and each component's doc block carries its own outcome.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playtest the card mechanic with real players</name>

  <read_first>
    - `.planning/ROADMAP.md` §Phase 3 Notes — *"the tiebreak rule and the play flow should be
      playtested with the target 4–8 friend group, not just implemented from the write-up."*
    - `.planning/phases/03-compiled-rules-priority-cards-swaps/03-RESEARCH.md` §Open Questions 4 —
      the two mechanics most likely to change on contact are D-18's rotation (does playing last
      actually feel like the advantage the design assumes?) and D-23's low-plays-first (players may
      expect high), and both are one-line changes in `selectCardPlayOrder` and `resolvePickOrder`.
  </read_first>

  <action>
    This task writes no code. Run the app for a real session, ask the four questions below, and
    record the answers verbatim. If the room wants a different rule, name the single function that
    would change and stop — do not change it here. A mechanic change after a playtest is a decision
    for the developer to take deliberately, not a fix to slip into a verification plan.
  </action>

  <what-built>
    The full card mechanic: cards `1..6` per player, played open in a rotating order, low plays
    first, ties to whoever played the value first, and the no-repeat rule enforced by making a
    stranding card unplayable.
  </what-built>

  <how-to-verify>
    Run at least two full rounds of card play with the real group, or with stand-ins if the group is
    not available, and answer four questions:

    1. Did playing last feel like an advantage? (D-18 assumes it does, which is what the rotation
       exists to distribute.)
    2. Did anyone expect **high** to pick first rather than low? (D-23.)
    3. Was the struck-through unplayable card understood without explanation, or did it need one?
    4. Did the resolved pick order stay findable during picking, or did anyone ask again?

    Both mechanics are one-line changes if the room disagrees — nothing in the contract hardcodes
    either, and the phase line reads its sentence from the same place the resolver does.
  </how-to-verify>

  <acceptance_criteria>
    - The summary records an answer to each of the four questions.
    - If a change is wanted, the summary names the single function that changes
      (`selectCardPlayOrder` or `resolvePickOrder`) and does not make it here.
  </acceptance_criteria>

  <resume-signal>Type "approved" or describe what the room wanted changed.</resume-signal>

  <done>All four playtest questions have a recorded answer, and any wanted mechanic change is named against its single function rather than applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan writes no source file and changes no runtime behaviour.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-48 | Repudiation | the DRFT-14 legibility claim | mitigate | 03-UI-SPEC states its arc-minute arithmetic as a prediction and names the falsifying pass. This plan runs it and records the screen tested, so the claim is evidence rather than arithmetic. |
| T-03-49 | Information disclosure | none | accept | No hidden information exists in this phase — every hand is public by CARD-07. Blind bans and their leak surface are Phase 4. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and modifies no file under `src/`. |
</threat_model>

<verification>
- `npm run verify` still exits 0 (nothing functional changed, but the phase closes green).
- `03-12-SUMMARY.md` records all three checkpoint outcomes, including the screen size and the
  screen reader used.
</verification>

<success_criteria>
- The three-metre pass is recorded per surface, with the screen tested named.
- The screen-reader outcome is recorded in the component doc blocks as well as the summary.
- The playtest answers are recorded, and any wanted change is named rather than made here.
</success_criteria>

<output>
Create `.planning/phases/03-compiled-rules-priority-cards-swaps/03-12-SUMMARY.md` when done.
</output>
