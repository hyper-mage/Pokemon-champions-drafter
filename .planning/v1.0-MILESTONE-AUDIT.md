---
milestone: v1.0
audited: 2026-09-02T14:55:00Z
status: tech_debt
scores:
  requirements: 92/94
  phases: 5/5
  integration: 8/8
  flows: 3/3
gaps:
  requirements:
    - id: "BAN-07"
      status: "partial"
      phase: "Phase 4"
      claimed_by_plans: ["04 plans declaring BAN-07 in requirements frontmatter"]
      completed_by_plans: ["04 SUMMARY.md files listing BAN-07 in requirements-completed"]
      verification_status: "passed (1 owner-approved override applied within criterion 4)"
      evidence: "Ships Partial by owner decision D-19. bothApply semantics are hardcoded in reduce.ts BANS_PLACED with no branch on the config field; the Re-ban arm is present in the UI but disabled. Integration checker confirms config.duplicateBanPolicy is written by ConfigScreen.tsx:1566, validated at import-guard.ts:807, migrated at migrate.ts:141-144, and read by nothing. Documented as deliberate at model.ts:330-343."
    - id: "EXPO-04"
      status: "partial"
      phase: "Phase 1"
      claimed_by_plans: ["01-08-PLAN.md"]
      completed_by_plans: []
      verification_status: "passed (requirement recorded as Correctly Pending)"
      evidence: "Requirement-text defect, not a code gap. The literal text - imports into play.pokemonshowdown.com and passes its team validator - is unsatisfiable by any species-only paste: Showdown's validator reports four inherent problems per Pokemon (no ability, no moves, 0 stat points) regardless of implementation correctness. The achievable, discriminating signal (no transforms in-battle error) is verified programmatically against the real pokemon-showdown@0.11.11 parser in docs/export-verification.md. No browser session against play.pokemonshowdown.com was run."
  integration: []
  flows: []
tech_debt:
  - phase: 01-draft-skeleton-on-a-real-url
    items:
      - "WR-01..WR-15: 15 warnings open exactly as disclosed; 3 criticals (CR-01/02/03) confirmed fixed in source"
      - "WR-09: heartbeat during the claim window is not counted, allowing a narrow dual-ownership write window"
      - "WR-10: a bfcache-restored secondary tab loses its stale watch permanently"
      - "IN-01..IN-06: 6 info findings open"
      - "PERS-05 cross-machine JSON round trip never exercised - only run on one machine"
      - "GitHub Actions pinned to floating major tags rather than commit SHAs"
      - "Remote branch master is stale and is still GitHub's default branch"
      - "No .gitattributes; every git add on Windows emits CRLF conversion warnings"
  - phase: 02-host-configured-draft-night
    items:
      - "02-REVIEW-FIX status partial: 4 of 5 in-scope findings fixed, WR-02 skipped (needed a real screen reader, since descoped)"
      - "6 info findings open"
      - "D1: nine pre-existing roster figures hardcoded in src/ doc comments"
      - "D2: the config screen has no way back to the landing screen"
      - "D3/WR-04: the reason names completion as the unlock, and at completion the pane stops holding the pool"
      - "D4/WR-06: the inert label measures 4.04:1 contrast; fix is a token change"
      - "D5/IN-01: reasonId is a document-global id derived only from key"
  - phase: 03-compiled-rules-priority-cards-swaps
    items:
      - "8 warnings + 4 info open; 2 criticals fixed"
      - "tests/ui/ban-list.test.tsx times out under full-suite parallel load"
      - "Two PoolGrids on the config screen keep independent densities"
      - "Three roster figures still written into src/ comments"
      - "Screen-reader verification DESCOPED for this milestone by host decision 2026-08-20"
  - phase: 04-blind-and-snake-bans
    items:
      - "tests/build/sw-manifest.test.ts is flaky under full-suite load"
  - phase: 05-full-tournament-brackets-standings-archive
    items:
      - "All 17 review findings fixed; no open findings"
      - "tests/build/sw-manifest.test.ts flaky under full-suite load (same as Phase 4)"
deferred_human_verification:
  - phase: "03"
    item: "Card-mechanic playtest with real players (D-18 rotation advantage, D-23 low-plays-first, struck-through card comprehension, pick-order findability)"
    status: "pending"
    reason: "Deferred to beta by explicit host decision 2026-08-19, re-confirmed 2026-08-20. To be run once the whole tool is complete rather than at the end of Phase 3."
bookkeeping_drift:
  - "REQUIREMENTS.md v1 checkboxes read 73/94 while its own traceability table reads 92/94 Complete. Flagged by 03-VERIFICATION.md:182-188 as documentation drift, not a functional gap."
  - "23 REQ-IDs are absent from SUMMARY.md requirements-completed frontmatter but are all SATISFIED with evidence in a phase VERIFICATION.md: CARD-01, CARD-03..08, DRFT-04, RULE-05, SWAP-02..07, SHEL-01, SHEL-02, PERS-04..07, EXPO-04, EXPO-06."
  - "Quick task 260813-tep SUMMARY.md has no status frontmatter field, so the SDK audit reports it [missing]. The body records delivered work and commits."
  - "STATE.md frontmatter reads completed_phases: 4 / percent: 80 while all 5 phases and 62/62 plans have summaries on disk."
nyquist: skipped
---

# Milestone v1.0 Audit — Pokémon Champions Drafter

**Audited:** 2026-09-02
**Status:** `tech_debt` — no blockers, accumulated deferred items need review
**Phases:** 1–5 (62 plans)
**Verify gate at audit time:** 81 test files, 2759 tests, 0 failures; typecheck clean; build clean; SW manifest 322 URLs / 1090 kB

## Milestone Definition of Done

From ROADMAP.md: *"You and your friends run a real draft plus bracket end to end in the tool, and nobody reaches for a Google Sheet or Discord message to track anything."*

ROADMAP names Phase 5 as the first phase satisfying this in full. Phase 5 is complete and verified.

## Requirements Coverage

Three-source cross-reference: REQUIREMENTS.md traceability table × phase VERIFICATION.md requirements tables × SUMMARY.md `requirements-completed` frontmatter.

| Source | Result |
|--------|--------|
| Traceability table rows | 94 |
| Appear in a phase VERIFICATION.md | 94 |
| **Orphaned** (assigned but never verified) | **0** |
| Satisfied | 92 |
| Partial | 2 (BAN-07, EXPO-04) |
| Unsatisfied | 0 |

| Phase | Requirements | Verified |
|-------|--------------|----------|
| 1 | ROST-01…12, SHEL-01…07, PERS-01…07, EXPO-01…06 | 32 (31 satisfied, EXPO-04 partial) |
| 2 | DRFT-01…03, DRFT-05…16, BAN-01, BAN-02, BAN-08, RULE-07 | 19 |
| 3 | DRFT-04, RULE-01…06, RULE-09, CARD-01…08, SWAP-01…07 | 23 |
| 4 | BAN-03…07, RULE-08 | 6 (5 satisfied, BAN-07 partial) |
| 5 | TOUR-01…09, PERS-08, PERS-09, REFR-01…03 | 14 |

### The two non-complete requirements

**BAN-07 — Partial by owner decision D-19.** The config field `duplicateBanPolicy` is written, validated on import, and migrated, but read by no reducer. `bothApply` is hardcoded; the `Re-ban` arm ships disabled. `model.ts:330-343` documents this as deliberate. This is a scope decision the owner already made, not a defect discovered by this audit.

**EXPO-04 — requirement-text defect, not a code gap.** Phase 1's verification argues the requirement as written cannot be satisfied by any species-only paste, because Showdown's validator flags four inherent problems per Pokémon for every such paste. ROADMAP's Phase 1 criterion 5 was already amended to the discriminating signal (no `transforms in-battle` error), and that is verified against the real parser. Recommended action is a reword of REQUIREMENTS.md, not a closure phase:

> *"Export imports into play.pokemonshowdown.com's teambuilder as the correct species and items, and a Mega slot produces no `transforms in-battle` validation error."*

## Phase Verification

| Phase | Status | Score | Overrides |
|-------|--------|-------|-----------|
| 01 Draft Skeleton on a Real URL | `passed` | 5/5 criteria; 31/32 requirements (1 correctly Pending) | 0 |
| 02 Host-Configured Draft Night | `passed` | 11/11 must-haves | 0 |
| 03 Compiled Rules, Priority Cards, Swaps | `human_needed` | 5/5 criteria; 23/23 requirements have code evidence | 0 |
| 04 Blind and Snake Bans | `passed` | 4/4 must-haves | 1 (owner-approved, within criterion 4) |
| 05 Full Tournament | `passed` | 5/5 criteria; 14/14 requirements | 0 |

Phase 03's `human_needed` resolves to exactly one outstanding item — the card-mechanic playtest with real players, deferred to beta by explicit host decision. Its two sibling items are settled: the screen-reader pass was DESCOPED by host decision 2026-08-20, and the 8-player board-height check PASSED 2026-08-20.

## Cross-Phase Integration

Eight seams traced through source. **No blockers found.**

| # | Seam | Result |
|---|------|--------|
| 1 | Config → Draft (2 → 3/4) | WIRED — one explicit `TournamentConfig` literal, no spread; every field has a reader except `duplicateBanPolicy` (BAN-07, above) |
| 2 | Bans → Pool (4 → 2) | WIRED, all three modes. `hostBanlist` filters at `ConfigScreen.tsx:1108-1111`; `blind`/`snake` union `selectAllBanIds` at `store.ts:437` before candidate filtering. BAN-08 holds |
| 3 | Rules/Cards/Swaps → Draft loop (3 → 2) | WIRED — UI calls the same selectors `canApply` judges against; no second derivation anywhere |
| 4 | Draft → Tournament (3/2 → 5) | WIRED — `tournament.ts` reads `DraftState` directly, no duplicated fold |
| 5 | Everything → Export (→ 1) | WIRED — `selectSlotStone` → `toShowdownPaste`; record separator is an unconditional blank line |
| 6 | Everything → Persistence (→ 1) | WIRED — `migrate.ts` v1→v5 chain matches `SCHEMA_VERSION = 5`; import guard has a named arm for all 17 action types, fields named explicitly; no `Set`/`Map`/`Date`/class instance reaches a persisted structure |
| 7 | Undo across phases | WIRED — `isUndoable` allow-list covers all Phase 2–5 kinds; `undoAnnouncement` uses a `const exhaustive: never` default, so a missing arm is a compile error |
| 8 | `seq` allocation | WIRED — `max(seq)+1` everywhere; every `log.length` hit is a doc comment, a last-element read, or a reverse-iteration bound, never a seq allocation |

## End-to-End Flows

| Flow | Result |
|------|--------|
| configure → pool → bans → draft with cards/swaps → export → bracket → results → archive | Traced end to end, no break. All screen transitions route through one `screenForState` function |
| Interrupt/resume — mid-draft, mid-ban, mid-bracket refresh | No gap. `startAutosave` is gated on document existence, not on screen; subscribes to the central store signal |
| Import a JSON file exported mid-draft / post-draft / mid-bracket | No break. `adoptTournament` always routes `migrate` → `fold`; guard coverage is stage-agnostic |

## Tech Debt Summary

| Phase | Open warnings | Open info | Notes |
|-------|---------------|-----------|-------|
| 01 | 15 | 6 | 3 criticals fixed; WR-09/WR-10 lock races open by design |
| 02 | 1 (WR-02, since descoped) | 6 | 4 of 5 in-scope findings fixed |
| 03 | 8 | 4 | 2 criticals fixed |
| 04 | 0 | 0 | resolved |
| 05 | 0 | 0 | all 17 findings fixed |

Cross-cutting: `tests/build/sw-manifest.test.ts` and `tests/ui/ban-list.test.tsx` are both flaky under full-suite parallel load. Neither failed in the audit-time run.

## Bookkeeping Drift (no code impact)

Four documentation inconsistencies, none affecting delivered behaviour. Worth fixing before archive so the milestone record reads accurately:

1. **REQUIREMENTS.md checkboxes** read 73/94 while its own traceability table reads 92/94 Complete. Already flagged inside `03-VERIFICATION.md:182-188`.
2. **23 REQ-IDs missing from SUMMARY frontmatter** — all satisfied with evidence in VERIFICATION.md. Affects `CARD-01`, `CARD-03…08`, `DRFT-04`, `RULE-05`, `SWAP-02…07`, `SHEL-01`, `SHEL-02`, `PERS-04…07`, `EXPO-04`, `EXPO-06`.
3. **Quick task `260813-tep`** SUMMARY.md has no `status:` field, so the SDK audit reports it `[missing]`. The work shipped.
4. **STATE.md frontmatter** reads `completed_phases: 4` / `percent: 80`; all 5 phases and 62/62 plans have summaries on disk.

## Verdict

The milestone meets its definition of done. Every requirement is traceable to a phase verification with code evidence, every cross-phase seam is wired, and all three end-to-end flows complete. Nothing found here blocks the archive.

What remains is a scope decision the owner already made (BAN-07), a requirement that needs rewording rather than code (EXPO-04), one human playtest deliberately deferred to beta, and an accumulated debt list that is fully disclosed rather than hidden.

---

*Nyquist validation skipped — `workflow.nyquist_validation` is `false`.*
